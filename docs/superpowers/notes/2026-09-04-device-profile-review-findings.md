# Device-profile design: adversarial review findings

**Date:** 2026-09-04. Four rounds of independent review that shaped `docs/superpowers/specs/2026-09-04-native-device-profile-design.md`. Round 1 reviewed the first draft (per-rung acceleration paths + a kernel micro-benchmark) against six subsystem maps; round 2 reviewed the written spec; round 3 re-checked the second draft finding by finding; round 4 read the third draft fresh. Findings are recorded as returned, severity-sorted, so the reasons behind each design decision stay readable. ggml facts refer to v0.22.0 (commit 34dc0e5589504286cb40e13cbdae4bf2b5b4071b) unless a finding says otherwise.


## Round 1 — first draft

Design under review: catalog rungs carry the acceleration path(s) they benefit from; native probes each path (CPU via `ggml_get_type_traits_cpu`, GPU via a timed MUL_MAT) once per device; the planner skips rungs whose path is slow or unsupported.


### Lens: probe-correctness (11)

- **[high] CPU axis is vacuous AND unreachable: vec_dot is non-NULL for every ladder type, and ggml_get_type_traits_cpu lives in the dlopen'd CPU module, not in anything libsokuji_native links** — The design answers cpu:* paths 'without timing' via ggml_get_type_traits_cpu(type)->vec_dot != NULL. In the pinned tree (ggml-src/src/ggml-cpu/ggml-cpu.c:217-408) the static type_traits_cpu table sets .vec_dot for F32, F16, BF16, Q4_K, Q5_K, Q6_K, Q8_0 and every other type the catalog ships (catalog.py:77 order_keys F16/Q8_0/Q6_K/Q5_K_M/Q4_K_M; TTS q8_0/bf16/f16 at catalog.py:1042-1143). The SIMD-vs-scalar decision is inside each vec_dot function via #ifdef, so the pointer test can never return 'no kernel' for any rung Sokuji publishes. Worse, with GGML_BACKEND_DL=ON + GGML_CPU_ALL_VARIANTS=ON (native/cmake/ggml_options.cmake:93-95) ggml-cpu is built as a MODULE (ggml-src/src/CMakeLists.txt:380-384, 486-509) — the stage dir holds libggml-cpu-armv8.*.so next to libggml-base.so — and ggml_get_type_traits_cpu (declared GGML_BACKEND_API in include/ggml-cpu.h:124, defined in ggml-cpu.c:417) is exported only from that module. It is not in the CPU registration's proc-address table either (ggml-cpu.cpp:652-683 lists set_n_threads, get_extra_bufts, get_features, set_abort_callback, numa_*, set_use_ref, threadpool_*). libsokuji_native links `ggml` and will fail to link, or resolve nothing at runtime because ggml dlopens modules RTLD_LOCAL. The repack tier (block-interleaved i8mm/AVX512 GEMM — the actual CPU speed differentiator for Q4_K/Q8_0, report §3) is gated in private traits.h and is invisible either way. Finally the planner already takes the SMALLEST quant on a CPU-only box (_tc_pick_quant docstring, planner.py:243) so no CPU answer can alter a recommendation.
  *Suggestion:* Drop cpu:* from the vocabulary and from sk_probe_accel. If a machine-level CPU fact is ever wanted, read it once from ggml_backend_reg_get_proc_address(cpu_reg, "ggml_backend_get_features") (this one IS in the proc table) and store it on Machine, not per rung. Do not attempt to call ggml_get_type_traits_cpu from the host library.

- **[high] 'vulkan:kquant_mmv' is a build-time constant with no reference path to compare against; the report's q6_k claim is wrong for the pinned tree** — The dedicated mul_mat_vec_<type>.comp shaders are a property of the wheel, not the device: mul_mat_vec_q2_k..q6_k.comp are all present in ggml-src/src/ggml-vulkan/vulkan-shaders/ (mul_mat_vec_q6_k.comp exists — the capability report §3 says q6_k is generic; it is not in v0.22.0). Every Vulkan device that runs this wheel gets the same shader, so a per-device probe of this path id can only ever report the same thing and there is no toggle to produce the 'generic reference' the design divides by — GGML_VK_DISABLE_* env vars exist for COOPMAT, COOPMAT2, INTEGER_DOT_PRODUCT, BFLOAT16, F16, MMVQ (ggml-vulkan.cpp:6232-6265, 6441, 7084-7087) but none for the dedicated K-quant mmv shader. The device-dependent Vulkan facts are only: integer_dot_product (→ mmvq/mmq), coopmat/coopmat2 (→ mul_mm_cm2), fp16, bf16 — and each is read once at instance/device init from the env, so a 'reference' timing needs a SEPARATE subprocess with the env var set, doubling the probe matrix.
  *Suggestion:* Delete kquant_mmv from the vocabulary. For the four real Vulkan variables either (a) parse the private booleans from the device-init log line 'ggml_vulkan: %zu = %s (%s) | uma | fp16 | bf16 | fp4 | warp size | shared memory | int dot | matrix cores' (ggml-vulkan.cpp:7333 — emitted at GGML_LOG_DEBUG, mapped to level 0 by sk_common.cpp:96 and to logging.DEBUG by native.py:17-20, so the sink must not filter it; pin the format per ggml pin bump), or (b) time absolute throughput per type and stop pretending a ratio exists.

- **[high] ggml picks the Vulkan matmul path per (vendor, architecture, K, N, type) at runtime — a fixed-shape probe measures a path the real model may never take** — ggml_vk_should_use_mmvq (ggml-vulkan.cpp:9434-9470): Q6_K uses mmvq only on Intel; on NVIDIA n==1 with k<=4096 never uses mmvq; Q8_0/MXFP4 mmvq only on NVIDIA_PRE_TURING; n>1 always mmvq when integer_dot is present. The mmv path is taken only for ne11<=8 (mul_mat_vec_max_cols=8, lines 389/10062) and the coopmat mul_mm 'aligned' path needs ne10==kpad && ne01>8 && ne11>8 (9246). So the probe's M=1/M=256 at some fixed K (K=4096 sits exactly on the NVIDIA threshold) can report 'integer_dot fast' for Q8_0 on an RTX 4070 while llama's decode of that same Q8_0 model goes through dequant mmv because ggml decided so. Conversely a catalog author cannot statically write 'this rung benefits from vulkan:integer_dot' because whether it does is a runtime function of the GPU vendor/arch and the model's hidden/FFN dims (Qwen3.5-4B, EuroLLM, the transcribe.cpp encoders and audio.cpp LLM heads all differ). llama.cpp's own n_batch=512 (sk_translate.cpp:198) and TTS families' small batches (2-8 columns) hit yet other branches.
  *Suggestion:* Probe with the model's actual K/N pairs (read from GGUF metadata or stored per card) and at the M the stage actually runs (1 for LLM decode, n_batch for prefill, ~1500 for ASR encoders), reporting absolute µs per call. Drop the per-rung 'benefits from path X' field; the sidecar cannot know which path ggml will choose without running it.

- **[high] A relative fast/slow tri-state cannot decide the absolute goal (aggregate RTF < 1); the 'slow' threshold is unspecified** — The design skips a rung whose path is 'slow' relative to the generic path on the same device. The goal is absolute: seconds of compute per second of speech across ASR+MT+TTS. A Q8_0 rung that is 'slow' (generic path) on an RTX 4070 SUPER still runs 10x faster than real time; a Q4_K_M rung that is 'fast' on an Intel Xe iGPU (Vulkan device with uma=1, mem_total = system RAM, so the fit-walk in _llamacpp_variant_row happily recommends the largest quant) may still not keep up. Nothing in the design states the ratio at which 'slow' flips, nor how a ratio composes across three stages. The existing bench cache already holds absolute rtf/tps/tts numbers (accel.py:533-596) — the probe as specified produces a value in a different unit that the planner cannot combine with them.
  *Suggestion:* Make the probe emit absolute numbers per ggml_type: GB/s at M=1 (bytes of weights read per µs) and GFLOPS at the prefill M. Then estimate tokens/s per rung = bandwidth / bytes-per-token from the card's known size, and compare against the per-stage budget the same way resolve_translate's E6 tps comparison already does. Specify the threshold as an RTF margin, not a ratio.

- **[high] Metal vocabulary never discriminates on any Mac Sokuji ships to; the timing probe there costs a subprocess + metallib compile for zero decisions** — metal:simdgroup maps to has_simdgroup_reduction, which is the precondition for EVERY MUL_MAT/MUL_MAT_ID on Metal (`return has_simdgroup_reduction && op->src[0]->type != GGML_TYPE_NVFP4;` ggml-metal-device.m:1709-1711) and is true on Apple7+ = every Apple Silicon; metal:bf16 (has_bfloat, Apple6/Metal3) likewise. mac-x64 has no Metal lane at all (ggml_options.cmake:23 — CPU only), mac-arm64 is Apple Silicon only. The only Metal device that differs is the paravirtual CI VM, already excluded by _paravirtual_metal_only (planner.py:106-118). What actually broke Metal in practice was op coverage — DIAG_MASK_INF and leading-edge PAD needed patches (ggml_options.cmake:69-89), ggml_sub needing src1 cont (audiocpp_compat.h) — none of which a MUL_MAT micro-benchmark can observe. Meanwhile GGML_METAL_EMBED_LIBRARY=ON (ggml_options.cmake:121) means each probe subprocess recompiles the embedded Metal source at init ('compiled in %.3f sec', ggml-metal-device.m:584).
  *Suggestion:* Skip the timing probe on Metal entirely. If Metal risk must be gated, gate on op coverage: build the family's real graph (or a per-family op list) and walk ggml_backend_dev_supports_op — that is the one check that would have caught the DIAG_MASK_INF/PAD aborts before R36-style rulings.

- **[high] MUL_MAT is not the op mix for TTS or ASR, and the TTS ladder is q8_0/bf16/f16 — not the Q4_K→f16 ladder the design describes** — Every TTS card carries only q8_0 and bf16 (or f16 alone for supertonic) — catalog.py:1042-1143 — so 'ladder Q4_K_M -> Q5 -> Q6 -> Q8 -> f16/bf16' has two rungs for TTS and the decision is exclusively 'is bf16 usable here'. On Vulkan bf16 IS the device-variable rung (device->bf16, coopmat_bf16_support/coopmat2_bf16_support, ggml-vulkan.cpp:3850-3853, 6253, 7148) — but the known bf16 defect on Vulkan is numerical (moss_tts_nano bf16 peak 1.228 clips, CLAUDE.md known gaps), not speed, and a timing probe hides it. TTS hot paths are conv1d/im2col (the compat shim exists precisely because im2col dtype differs), flash-attention (Vulkan FA has coopmat2/coopmat1/scalar variants chosen per device), get_rows and the codec decoder; ASR encoders run M≈1500 prefill matmuls plus convs. A single-backend MUL_MAT at two shapes says nothing about whether FA falls to the scalar path or conv lands on a slow kernel on a given GPU.
  *Suggestion:* Scope the probe honestly: it can only rank LLM-style decode/prefill matmul for translate. For ASR/TTS keep the existing measured-RTF path (measure_rtf / measure_rtf_tts) as the only source of truth, and gate the bf16 TTS rung on a numerical check (compare a short synth against the q8_0 rung) rather than a timing ratio.

- **[medium] Cache key (device description, native version) misses the driver/ICD, env overrides, and the ggml pin — the facts that actually flip Vulkan paths** — ggml_backend_vk_get_device_description returns VkPhysicalDeviceProperties.deviceName only (ggml-vulkan.cpp:17898-17901) — no driverVersion, no driverID. coopmat gating is driver-dependent: AMD proprietary/AMDVLK → RDNA3 only, RADV → all; Intel Xe1 iGPU → only on the Windows proprietary driver (18940-18950). Switching RADV↔AMDVLK, a driver update that adds KHR_shader_bfloat16, or installing MoltenVK/lavapipe (device dedup + driver_priorities at 7494-7549) all change the answer under the same key. GGML_VK_DISABLE_*/GGML_VK_VISIBLE_DEVICES/GGML_VK_FORCE_MMVQ (6185-6459, 7084-7087, 7444) alter behaviour and device ordering and are not keyed. 'native version' must be the full sk_engine_versions string (ggml/lane pins, sk_common.cpp:153-166), not sk_version alone — note Machine.fingerprint today contains neither (accel.py:150-156). On macOS the OS version drives Metal compiler output.
  *Suggestion:* Key the probe on: machine.fingerprint + sk_engine_versions() + a per-device driver identity (add driverID/driverVersion/deviceUUID to sk_device — needs a new native accessor since ggml's public API does not expose them; the init log line at 7333 is the only free source) + the GGML_VK_* environment slice. Store the key material alongside the value so a stale entry is explainable.

- **[medium] Multi-GPU / iGPU+dGPU: the probe, the budget, and the load can each target a different device** — native.device_for(kind) returns the FIRST device of that kind (native.py:39-45); _quant_budget_bytes uses the LARGEST mem_total across machine.gpus (planner.py:227); Plan.device is the bare string 'vulkan' with no index. On a laptop with an Intel/AMD iGPU (uma=1, mem_total = system RAM) plus an NVIDIA dGPU, ggml-vulkan's ordering after UUID dedup and driver_priorities (7444-7549) can put the iGPU first: budget comes from the dGPU, the probe keyed by description runs on whichever the sidecar picks, and the load lands on the iGPU. ggml_backend_vk_init(size_t dev_num) (17870) takes a Vulkan-relative index, not sk_device.index — the exact trap sk_tts.cpp already documents at backend_relative_index (sk_tts.cpp:157-161). sk_init also lists IGPU-type devices as placement targets (sk_common.cpp:207-209), so this is not hypothetical.
  *Suggestion:* Have sk_probe_accel take the flat sk_device.index and open the backend via ggml_backend_dev_init(devs[index], NULL); carry the device index on Plan and on the probe result; make _quant_budget_bytes and device_for agree on one primary device. Treat uma=1 devices as 'unified' for the fit-walk the way Apple Silicon already is.

- **[medium] Probe cost is a per-subprocess fixed cost multiplied by path ids, with lazy per-shape pipeline compiles and no ggml-side pipeline cache** — Each subprocess pays: Python start + sokuji_native import + sk_init → ggml_backend_load_all_from_path (dlopens every CPU variant, Vulkan instance init and device enumeration; Metal compiles the embedded library). Vulkan pipelines are then compiled lazily per requested (type, M-bucket, wg size) through CompileTask threads (ggml_vk_load_shaders(device, requested), ggml-vulkan.cpp:2071/4164, 6143); grep finds no vk::PipelineCache in the backend, so cold-start cost depends solely on the driver's disk cache (absent on a fresh Windows install, first run after a driver update). The signature sk_probe_accel(device, path_id) implies one call — and, if each is containment-isolated, one subprocess — per path id per device: 5-8 path ids × devices × (two shapes × N iterations + compile). Nothing states when this runs relative to the first session; if it runs at hardware_info time it lands on the user's first click, and if it runs while a stage is loaded the timed loop shares the GPU with the live model. GPU DVFS: a median taken right after a short warm-up on a laptop in a low clock state yields a 'slow' verdict cached forever.
  *Suggestion:* One subprocess per device that answers ALL path ids (and both baselines via env) in a single sk_init; run it at idle after first launch with a deadline, never while a stage is loaded; warm up until timings stabilise (coefficient of variation bound), not a fixed count; report elapsed wall time per device in the cached record.

- **[medium] Crash containment conflates 'unsupported' with transient failure and can take the live session down with it** — A non-zero exit from the probe subprocess is ambiguous: GGML_ABORT on an unsupported op, a vk::OutOfDeviceMemoryError escaping as std::terminate when the probe allocates while the sidecar's real models hold VRAM, a driver crash, or a Python import failure all look the same, and the design caches the result per (description, native version) with no retry/expiry — a one-off OOM becomes a permanent 'unsupported' that hides a rung until the next wheel bump. ggml-vulkan writes some diagnostics to std::cerr rather than the ggml log (7455), so the parent must capture the child's stderr, not only the log sink. On Windows a hung compute shader triggers a TDR that resets the adapter for every process — the parent's live Vulkan context receives VK_ERROR_DEVICE_LOST — so 'subprocess' contains the SIGABRT but not the GPU reset.
  *Suggestion:* Cache 'unsupported' only when the child's captured log contains the GGML abort text naming the op; treat other non-zero exits as 'unknown' with bounded retry and an expiry. Run the probe only when the VRAM ledger (_LEDGER) is empty and no session is active. Bound each graph_compute with a watchdog well under the OS TDR threshold (2 s on Windows) so a stuck shader is killed by the parent before the driver resets the adapter.

- **[low] The closed tri-state cannot express the outcome that actually happens: scheduler reroute to CPU** — When a (op, type) is unsupported on the GPU inside a real llama.cpp/audio.cpp graph, ggml_backend_sched silently reroutes that node to the CPU backend (report §4 pitfall c) — the model still 'runs on GPU' from the sidecar's point of view (Plan.device = vulkan, ReadyMsg.device = vulkan) but with a per-token host round trip. The design's single-backend probe reports 'unsupported' for that case while the runtime outcome is 'supported, slow, partially on CPU'; the vocabulary has no value for it, and the renderer reason 'no accelerated path on this GPU' would be shown for a rung that in fact loads and runs. The reverse also occurs: a rung the probe marks 'slow' at M=256 may still be the fastest available for decode (M=1), where the ladder direction (smallest-first, opposite to the existing largest-first fit-walk in _fit_walk/_llamacpp_variant_row) then discards the better rung.
  *Suggestion:* Report per-rung an estimated tokens/s (or RTF) and let the planner threshold it; expose to the renderer a reason code plus the measured number ('~4 tok/s on this GPU') instead of a categorical path verdict.


### Lens: planner-integration (12)

- **[high] Filter placed before the downloaded-rung intersection silently switches to an undownloaded file** — Both fit-walk callers apply the `downloaded` restriction as an intersection over the candidate dict and fall back to the FULL dict when the overlap is empty: `_llamacpp_variant_row` (planner.py:518-527, `cached = {q: sz for q, sz in quants.items() if q in downloaded}; if cached: quants = cached`) and `_tc_pick_quant` (planner.py:264-269, same shape over `sizes_all`). If the new 'unsupported|slow' filter narrows `quants`/`sizes_all` first and the user's already-downloaded rung is the one probed slow, `cached` is empty, the code keeps the filtered set, and `_fit_walk` returns a rung that is NOT on disk. That is exactly the rule `_quant_budget_bytes`'s docstring forbids ('we always run exactly the file the user downloaded ... never a silent switch to a different model file') and it flips `test_resolve_tts_downloaded_restricts_the_fit_walk`, `test_resolve_asr_prefers_downloaded_quant_over_fresh_recommendation`, `test_resolve_translate_prefers_downloaded_quant_over_fresh_recommendation`, and the `downloaded_listed_only_quant_wins` / `downloaded_restricts_to_cached_quant` rows of TC_PICK_QUANT_DIRECT_MATRIX. `_tc_pick_quant` already has the right precedent: the curated filter shapes only `sizes` (fresh recommendations) while `sizes_all` feeds pin and downloaded.
  *Suggestion:* Specify that the accel filter is a FRESH-RECOMMENDATION filter only: in `_tc_pick_quant` apply it to `sizes` (never `sizes_all`); in `_llamacpp_variant_row` apply it only when the downloaded intersection is empty (or intersect first, filter second, and never let the filter empty a non-empty `cached`). Add a planner test: downloaded={slow rung} on a GPU machine must still return that rung.

- **[high] 'Unsupported' is skipped in the walk but still lands on the GPU tier through pin, downloaded, and override='gpu'** — Three paths bypass the recommendation walk and choose a (quant, tier) directly: `_tc_pick_quant` returns `pin` before any gating (planner.py:262 `if pin in sizes_all: return pin`); `_llamacpp_variant_row` returns `_row(pin)` which picks the best `_tier_available` tier (planner.py:497); `resolve_deployments`'s `_pinned` for override 'gpu' pins ANY accelerator row (planner.py:166-172) after `_llamacpp_quant` narrowed to the rank-default quant with no fit logic. The design only says the walk 'skips' an unsupported rung, so all three still place it on gpu-vulkan/gpu-metal. The consequence differs per stage and the design does not say which: sk_translate.cpp loads with `n_gpu_layers = -1` under llama.cpp's scheduler, so an op the GPU backend rejects silently runs on CPU (slow, not fatal); sk_tts.cpp has no scheduler and an unsupported op is a GGML_ABORT/SIGABRT (R36 evidence, planner.py:95-103). So 'unsupported' for TTS is a crash that pin/override can still reach.
  *Suggestion:* Define 'unsupported' as a per-(compute_type, tier) availability fact enforced where tiers are gated (a sibling of `_tier_available` that takes the Deployment, used by `resolve_deployments`'s `usable` filter, `_row()` in `_llamacpp_variant_row`, `select_variant.candidate`, and both `gpu_possible` checks), so pin/downloaded/override resolve to that rung's cpu row instead of aborting. Keep 'slow' as recommendation-only.

- **[high] Recommendation and runtime pick are the same functions called with different arguments from three sites, plus an override path with no walk at all** — `_h_models_catalog` computes `recommended` via `_llamacpp_variant_row(mdl, m, None, 0, budget)` / `_tc_pick_quant(mdl, m, None, budget)` (accel.py:806-810) with no `downloaded` and no reserve; `_h_list_variants` (legacy, still used by NativeModelClient.ts/nativeModelStore.ts) calls `select_variant(model, m, reserve, pin, budget_bytes=...)` with no `downloaded`; `resolve`/`resolve_translate`/`resolve_tts` wrappers (accel.py:230-262) pass `_downloaded_quants`. The explicit-override branch of `resolve_translate`/`resolve_tts` uses `_llamacpp_quant` (rank-default, planner.py:449-463) and then `resolve_deployments` — neither touches `_fit_walk`. Because planner.py is pure and `Machine` is frozen, probe results must arrive as a parameter: either a new `Machine` field (touching every `Machine(...)` fixture in test_planner.py, test_accel.py, test_characterization.py, test_platform_filter.py and `probe()`'s assembly) or a new kwarg on `_tc_pick_quant`, `_llamacpp_variant_row`, `select_variant`, `resolve`, `resolve_translate`, `resolve_tts`, `resolve_deployments` and their accel.py wrappers. The design's step 3 names only 'the planner's recommendation walk'.
  *Suggestion:* Enumerate the four call sites explicitly in the design and pick one injection mechanism. Add a consistency test asserting `_h_models_catalog`'s `recommended` equals `resolve_*`'s first plan compute_type when nothing is downloaded, on a fixture with a synthetic probe result.

- **[high] 'Runtime device placement continues via measured RTF' does not hold: TTS auto ignores the cache, and the CPU comparand is never measured in auto mode** — `planner.resolve_tts`'s auto branch never reads `cache` (planner.py:401-412) — the only bench use is `_apply_bench`, which runs inside `resolve_deployments` (ASR always; translate/tts only on explicit override), and the E6 tps swap in `resolve_translate` (planner.py:346-353). Both require BOTH the GPU entry and the CPU-floor entry (`_apply_bench`: `if cpu_rtf is None: return plans`). But `measure_rtf`/`measure_tps`/`measure_rtf_tts` run only on the plan that actually loaded (asr_engine.py:71, translate_engine.py:82, tts_engine.py:146), so a CPU entry exists only after the user once forced override='cpu' or a GPU load failed. In auto mode the ratchet never has the number it needs and never demotes. The design leans on this mechanism for the 'keeps up' half of its goal.
  *Suggestion:* Either extend `resolve_tts` auto to apply `_apply_bench` and add a one-off CPU-floor measurement policy (e.g. measure the cpu plan once when a GPU plan's RTF is >= a threshold), or drop the claim and make the accel probe the only speed signal. State which.

- **[high] The MUL_MAT probe cannot see the failure R36 encodes, and 'generic reference path on the same device' is not selectable through ggml's public API** — The paravirtual-Metal abort is on GGML_OP_NORM/RMS_NORM/ARGMAX (missing has_simdgroup_reduction; planner.py:95-103), not on MUL_MAT — a MUL_MAT-only `sk_probe_accel` reports 'fast' there, so `_paravirtual_metal_only` must stay and the probe cannot supersede it (contrary to the tests map's suggestion). For Vulkan, `ggml_backend_vk_device_supports_op` accepts Q4_K/Q8_0/BF16 MUL_MAT on every device (ggml-vulkan.cpp ~18140-18170), the mmv q4_k/q5_k/q6_k shaders are compiled into every device, and `.get_proc_address = NULL` (ggml-vulkan.cpp:18851) — there is no feature query and no knob to force the non-mmv/non-coopmat pipeline, so 'ratio vs the generic reference path' is only measurable across TYPES (bf16 vs q8_0 vs f16), which measures bytes moved, not whether an acceleration path exists. Metal DOES export `ggml_backend_get_features` via get_proc_address (ggml-metal.cpp:901-903), so Metal needs no timing at all — the design has it backwards for that backend.
  *Suggestion:* Split the probe: Metal and CPU answer from `ggml_backend_get_features` (include simdgroup_reduction/bfloat so R36 gets a structured signal alongside the string match); Vulkan answers from `VK_KHR_shader_integer_dot_product` / coopmat bits read via a small sk_ call into the device props instead of a timing race. If timing stays, define the comparand as another catalogued rung of the same model (q8_0 vs bf16), and probe at least one normalisation op so a paravirtual-class device can fail it.

- **[high] One ladder does not fit three stages, and it walks onto rungs the catalog forbids recommending** — The ladder Q4_K_M→Q5→Q6→Q8→f16/bf16 exists only for ASR (`_tc_row`, catalog.py:66-83), where q5_k_m and f16 are rank 0.5 listed-only and `_tc_pick_quant` excludes them from fresh recommendations (`_TC_CURATED_MIN_RANK`; TC_PICK_QUANT_DIRECT_MATRIX row `fresh_recommendation_never_listed_only`). Translate cards have exactly {q4_k_m, q8_0} (`_llm_translate_row`); TTS cards have {q8_0, bf16|f16} with supertonic (f16 only) and voxcpm1 (q8_0 only) single-rung (`_tts_gguf_row`). For TTS the whole decision is bf16 vs q8_0, and the path vocabulary answers 'fast' almost everywhere: Metal `has_bfloat` is true on every real Apple GPU (Apple6+/Metal3, ggml-metal-device.m:1050-1053; only the paravirtual shim clears it), Vulkan creates the BF16 dmmv pipeline unconditionally (ggml-vulkan.cpp:5243). Meanwhile `_fit_walk` is largest-first, so every GPU fixture in TTS_MATRIX resolves bf16 — a rung that moves ~1.7-2x q8_0's bytes at M=1 and is strictly slower on any bandwidth-bound device. The design's own assumption ('published ⇒ quality is close enough, speed decides') argues for smallest-that-keeps-up, which contradicts both 'preferring quality among rungs that keep up' and the text's 'smallest first' vs `_fit_walk`'s largest-first. A binary fast/slow/unsupported cannot encode 'both fast, q8_0 faster'.
  *Suggestion:* Decide per stage: keep ASR's curated set and largest-fit (K-quant paths matter there); for TTS/MT make the rule 'smallest rung whose path is fast' or drop the accel axis and use measured per-model RTF. Say explicitly whether the ladder respects `_TC_CURATED_MIN_RANK`.

- **[medium] No 'unknown probe result' semantics — the frozen characterisation suite and every synthetic Machine depend on it** — CPU_ONLY/CUDA_12GB/CUDA_24GB/APPLE_SILICON in test_characterization.py (and every `_nv_machine`/`Machine(...)` fixture elsewhere) carry no probe data. If 'no result' is treated as anything other than pass-through, every GPU row in TTS_MATRIX (all bf16), ASR_MATRIX and TRANSLATE_MATRIX flips, which the file's docstring forbids without justification. Separately, the suite calls the `accel.resolve*` wrappers with an autouse pin only on `_downloaded_quants`; if the wrapper reads accel-bench.json for cached probe results, the frozen suite becomes dependent on this box's ~/.cache — the exact host-dependence the docstring's determinism note guards against.
  *Suggestion:* Specify: absent/unknown probe result == 'generic' (no filtering). Add an autouse fixture in test_characterization.py pinning the probe cache to empty, mirroring the `_downloaded_quants` pin.

- **[medium] Probe cache key and trigger point are underspecified: fingerprint lacks native version and driver version, and a lazy probe stalls the first models_catalog RPC** — `Machine.fingerprint` (accel.py:152-156) hashes os/arch/apple/installed/tc_kinds/(kind:desc:mem_total) — no `sk_version()`, so a native bump does not invalidate bench entries; the design's (description, native version) key fixes that for probe rows but the Vulkan device description carries no driver version, and coopmat/integer-dot availability on NVIDIA/AMD changes with the driver. `bench_load`/`bench_save` are whole-file last-write-wins with no locking, and a subprocess writer racing `_measure` during a load loses updates. The design does not say WHEN the probe runs: `_h_models_catalog` is called on every Settings open; if it lazily runs N subprocesses each paying the first-time Vulkan pipeline compile (2-14s, W-1) the RPC blocks and the renderer shows an empty library.
  *Suggestion:* Key probe rows on (fingerprint, native version, driver/API version string from the device props) and write them from ONE place (sidecar startup or a dedicated RPC), never from `_h_models_catalog`; have the handler read only. Give the probe file its own path or an atomic rename.

- **[medium] Renderer has only two per-rung states; 'not recommended here' cannot ride `supported=false` without making the rung unpickable** — `VariantDropdown` renders `disabled={!v.supported}` and only calls `onPinVariant` when `v.supported` (NativeModelManagementSection.tsx:112-131); `deriveVariantRepos` (nativeModelStore.ts:216-228) drops a persisted pin whose variant is `supported === false`. `_h_models_catalog` sets `supported: True` unconditionally for GGUF cards (accel.py:817) and `variants[]` has no reason field; `variantData` (lines 523-550) fabricates a memory reason for every unsupported rung. So if 'no accelerated path on this GPU' is carried as supported=false the rung becomes unpickable and a user's pin is silently dropped — contradicting the planner's 'pin wins' — and `test_catalog_variants_translate_kind_included` (all supported True) flips.
  *Suggestion:* Add an optional `reason?: string` (or `reasonKey`) to `NativeModelInfo.variants` and a distinct 'notRecommended' flag; keep `supported` for won't-load only. Have `variantData` read the wire reason before falling back to its memory string, and render it on enabled options too.

- **[medium] No card-level state for 'nothing keeps up on this machine'** — catalog.py:970-984 records voxcpm2/index_tts2 at M4 Metal RTF 1.42/1.77 with CPU 3.27/4.94 — no rung and no device meets RTF<1, yet `_TTS_TIER_OVERRIDES` lists Metal and the planner recommends bf16 on gpu-metal. The design's per-rung reason ('no accelerated path on this GPU') is not the reason here; the card simply cannot keep up. Likewise the aggregate RTF<1 across ASR+MT+TTS has no computation site: `measure_rtf` (3s sine), `measure_tps`, `measure_rtf_tts` each run alone on an idle device, sessions run all three concurrently, and `_LEDGER` tracks bytes only.
  *Suggestion:* Add a model-level 'will not keep up' state derived from measured RTF (or a catalogued per-device RTF table like the one already in the comment) and surface it on the card, separate from the per-rung accel reason. Defer the aggregate-RTF goal or define how three isolated numbers combine.

- **[low] The catalogued per-rung path field cannot vary by tier inside the existing row builders** — `_tc_row`, `_llm_translate_row`, and `_tts_gguf_row` each emit `Deployment(..., tier, quant, ...)` in a `for tier in tiers` loop with identical fields per tier. A q4_k_m row needs `vulkan:kquant_mmv` on gpu-vulkan, a Metal id on gpu-metal, and `cpu:dotprod`/`cpu:i8mm` on cpu — different path ids for the same quant. The 'small field on Deployment' therefore has to be a per-backend map (the design's 'per backend' wording) or the builders must be rewritten; `_tier_available(tier, machine, backend)` already accepts `backend` and ignores it (planner.py:121), so a sibling taking the Deployment is the natural seam. Tests that pin row shape: `test_omnivoice_card_shape`, `test_tts_tier_overrides_default_is_cpu_only_for_unknown_family`, and the rank-convention tests in test_catalog.py.
  *Suggestion:* Make the field `accel_paths: Mapping[tier, tuple[path_id, ...]]` with default empty, defaulted in `Deployment` so every existing constructor call and test fixture stays valid.

- **[low] Tests that would flip or need new cases** — Must NOT flip: `test_tier_available_gpu_metal_excluded_on_a_paravirtual_device` and `test_paravirtual_exclusion_removes_gpu_metal_plans_end_to_end` (R36 stays); every row of TTS_MATRIX/ASR_MATRIX/TRANSLATE_MATRIX and TC_PICK_QUANT_MATRIX/LLAMACPP_VARIANT_ROW_MATRIX given unknown-probe pass-through; `test_catalog_variants_cpu_only_recommends_smallest` and `cpu_only_smallest_wins_ignores_budget` (the no-GPU→smallest rule is orthogonal since CPU `vec_dot` is non-NULL for every catalogued type — ggml-cpu.c:310-316 — so CPU can never be 'unsupported'). Will flip if semantics change: `test_list_variants_marks_supported_and_recommended`/`test_list_variants_dedupes_llamacpp` (`reason: "ok"`), `test_catalog_variants_translate_kind_included`/`test_catalog_variants_full_ladder_sorted_quality_desc` (all supported True), and `_FakeDev`/`_fake_native_module` positional constructors in test_accel.py:917-940 if `Device`/`Machine` grows a field. TS suites (nativeCatalog.test.ts, nativeModelStore.test.ts) are unaffected while new wire fields stay optional.
  *Suggestion:* List these in the design's test plan and re-record any characterisation row by running the code, per that file's docstring.


### Lens: product-and-ops (13)

- **[high] Smallest-first ladder flips every existing recommendation and makes already-downloaded models read as 'not downloaded'** — Today `_llamacpp_variant_row`/`_tc_pick_quant` (sidecar/sokuji_sidecar/planner.py) recommend the LARGEST rung that fits, and `_h_models_catalog` (accel.py:800-835) documents `recommended` as 'the stable default-download pick'. The design inverts this to Q4_K_M-first. On the renderer, `deriveVariantRepos` (src/stores/nativeModelStore.ts:216-223) computes each card's status repo as `pinned ?? recommended`; a user who downloaded the recommended rung has NO pin recorded. After the update the recommendation moves to q4_k_m, `model_status` is queried against the q4_k_m repo, the card flips to 'absent', the Download button reappears, `ensureSelectionReady` treats the stage as not ready, and the provider chip shows 'None' — the exact Fun-ASR regression the docstring at nativeModelStore.ts:195-201 was written to prevent. Meanwhile the sidecar's load path (`_downloaded_quants` restrict-if-overlap) would happily keep loading the q8_0 on disk. It also re-records every tuple in sidecar/tests/test_characterization.py (frozen by its own docstring) and contradicts `_h_models_catalog`'s 'must never flap → re-downloads' contract.
  *Suggestion:* Treat a rung already in the HF cache as the recommendation for that card (downloaded-wins, mirroring `_downloaded_quants` at load) so status never flips underneath a working install; only fresh cards get the new ladder. Ship an explicit migration note in the characterization re-record. Consider auto-writing a pin when the user downloads a rung so later recommendation moves are inert for them.

- **[high] Probe result cannot predict whether a model keeps up; the shipped catalog already has models that cannot keep up at ANY rung on any device** — sk_probe_accel returns a kernel-level ratio (fast/slow vs a generic path), not absolute throughput. The M4 numbers (voxcpm2 RTF 1.42, index_tts2 1.77 on Metal) are not accel-path failures — Metal simdgroup would probe 'fast' — they are model-compute failures, and the ladder would still recommend a rung. Worse, catalog.py:746-754 says voxcpm1/voxcpm2/irodori_tts/index_tts2 are absent from `_TTS_TIER_OVERRIDES` and resolve cpu-only today, where they are slower still. So four shipped cards accumulate lag on every machine, and nothing in the design (or the current UI — `formatRtf` in nativeCatalog.ts:307 renders rtf 1.42 as a neutral '0.7× realtime' with no warning class) tells the user before or after download. The ladder 'then existing fit-with-headroom rules' has no outcome for 'no rung keeps up here'.
  *Suggestion:* Add a per-(family, quant, device-class) published RTF table to the catalog (from the fleet runs already recorded in catalog.py comments) so `recommended` can become 'none — too slow for live speech on this machine' pre-download, and add a post-measure surface: when a stage's ready rtf ≥ ~0.8 (or aggregate ≥ 1) raise a warning state on the owning store rendered on the card/badge, not just a log line.

- **[high] Aggregate RTF (ASR+MT+TTS < 1) is not computable from what ready payloads carry** — ReadyMsg (nativeProtocol.ts:2-6) carries `rtf` for ASR/TTS and `tokensPerSec` for translation. (1) MT has no RTF: converting tps needs an expected tokens-per-second-of-speech (language-dependent — ja/zh targets tokenize 2-3x denser than the en→fr benchmark sentence in `measure_tps`) plus prefill cost, which for short utterances dominates and is not in tps at all. (2) ASR rtf is measured on a 3 s sine tone (`measure_rtf`, accel.py:533-546); the native roster includes autoregressive audio-LLM decoders (catalog.py:292/308/328) whose cost scales with emitted tokens, so a near-silent clip understates real-speech RTF. (3) TTS rtf is gen_time / OUTPUT audio seconds; the budget needs it scaled by translated/source duration ratio. (4) All three are measured in isolation with the GPU to themselves; in a session they contend on one device. The sum is therefore neither computable nor a safe bound.
  *Suggestion:* Define the budget explicitly: measure ASR on a bundled short speech clip, benchmark MT as ms-per-source-second (fixed source utterance, target language of the session, including prefill), report TTS as gen_ms per SOURCE second using a fixed translation pair, and put all three on the wire as `rtf` with a `contended: false` flag. Until then, do not present an aggregate number in the UI.

- **[high] Cached probe and bench entries do not invalidate on a native version bump; device description alone is the wrong key** — Machine.fingerprint (accel.py:152-157) hashes OS/arch/installed/tc_kinds/(kind, description, mem_total) — not `sk_version()` or engine pins. The design keys the new probe by '(device description, native version)' but says nothing about the EXISTING rtf/tps/tts entries that the 'measure-then-adapt' half depends on; after native 1.0.3 changes a kernel, the placement decision still reads 1.0.2 numbers forever. Description is also insufficient on its own: the same GPU under RADV vs AMDVLK vs proprietary NVIDIA, or a Mesa update, gains/loses coopmat/integer_dot support with an identical description string. The cache file lives in `~/.cache/sokuji-sidecar/accel-bench.json` (accel.py:486), outside userData, so it survives uninstall/reinstall and is shared between a dev checkout and the packaged app.
  *Suggestion:* Fold `native.module().version()` + engine_versions + the backend's driver/API version (ggml_backend_dev_props / Vulkan driverVersion+driverName, Metal OS build) into a single cache-generation prefix applied to every namespace ('', 'tps:', 'tts:', and the new probe ns), and move the file under the sidecar bundle's userData dir so a bundle upgrade or uninstall clears it.

- **[high] First-run probe blocks the Settings model library and Start button with no progress UI** — The design runs probes 'for every path id referenced by any catalogued rung' at catalog time. `_h_models_catalog` is on the sidecar-ready critical path: nativeModelStore.ts:440-470 awaits three models_catalog calls before setting `sidecarStatus: 'ready'`. Each Vulkan/Metal probe is a subprocess that must dlopen the wheel, `sk_init` (loads all backend modules), and compile a MUL_MAT shader per ggml type — on NVIDIA the cold pipeline-cache compile is 2-14 s per kernel (catalog.py:930-932, W-1); with ~5 Vulkan paths × 2 shapes × N repeats, first launch after install AND after every sidecar bundle update could sit for 30-60 s with the library empty and the user staring at a spinner (or worse, at 'unavailable' if a WS timeout fires). There is no spec for a timeout, a progress message, or what the UI shows while results are pending.
  *Suggestion:* Run probes lazily off the critical path: emit the catalog immediately with `accel: 'pending'` per rung, run probes in a background task, push a `models_catalog_result` refresh when done, and gate only the 'Recommended' star on completion. Cap each subprocess with a hard timeout and cache a timeout as 'unknown' (not 'unsupported').

- **[medium] An aborting or hanging probe subprocess is not cached, so it re-runs every catalog fetch; probes must honour the paravirtual exclusion** — A GGML_ABORT in the probe child is a SIGABRT; the design states the subprocess isolation but not the result mapping. If an abort maps to 'unsupported' but is not persisted (e.g. because the parent only writes successful results), every `models_catalog` call re-spawns a crashing child. A hung driver (Intel iGPU Vulkan is a known offender) blocks the executor with no timeout. Also, `_tier_available` excludes /paravirtual/i Metal devices (planner.py:106-142) precisely because a plan there hard-aborts — the probe must consult the same gate or CI/VM users get an aborting subprocess per path on every launch.
  *Suggestion:* Persist abort/timeout outcomes as first-class statuses ('unsupported' for SIGABRT, 'unknown' for timeout) under the same cache key; skip devices `_tier_available` rejects; capture the child's stderr (which carries the aborting op name via `log_line`) into the cached record for bug reports.

- **[medium] 'Not recommended here' needs a third variant state and a reason CODE on the wire, not free text** — VariantDropdown (NativeModelManagementSection.tsx:135-140) renders a reason ONLY for `!v.supported` rungs, which are also disabled — there is no 'selectable but not recommended' rendering. If the new rung state is expressed as `supported: false`, the user cannot pin a rung that would run (just slower), and the `!gpuFits` note 'No GPU variant fits — runs on CPU' (line 148) becomes a lie. `NativeModelInfo.variants` (nativeProtocol.ts:41) carries no reason field, and the legacy `list_variants_result` path sends English free text ('no usable GPU', accel.py:659-663). All five existing models.* keys are present in 30 locale files; a sidecar-authored string would be the first un-localised user-facing text on the card, and the card already has one hardcoded 'Requires GPU' span (line 366) as a precedent not to repeat.
  *Suggestion:* Add `reason?: 'no_accel_path' | 'accel_slow' | 'wont_fit' | ...` (a closed enum) plus `accelStatus` to the variants wire shape; keep `supported` = 'can load'; render a non-disabled option with a muted reason line and keep the pin allowed. Add `models.variantNoAccelPath` / `models.variantAccelSlow` to all 30 locales in the same PR.

- **[medium] When every GPU rung is skipped, the renderer silently crowns the largest download as 'Recommended'** — `_h_models_catalog` marks `recommended: ct == rec`; if the ladder skips every rung on the target device and the sidecar emits no recommended entry, both `variantData` (NativeModelManagementSection.tsx:546) and `deriveVariantRepos` (nativeModelStore.ts:223) fall back to `vs[0].id` — and `vs` is sorted size-DESCENDING, so the biggest file (bf16 / f16) becomes the default download and the status repo. Whole-card gating (`hardwareGated`, nativeCatalog.ts:221) is tier-level and unchanged by the design, so the card also looks fully available.
  *Suggestion:* Make the sidecar always emit exactly one `recommended` (falling back to the CPU-tier pick, smallest rung, when no GPU rung qualifies) and change both renderer fallbacks from `vs[0]` to the smallest rung; add a `recommendedTier` so the card can show 'runs on CPU here'.

- **[medium] 'Measure-then-adapt' never fires: the CPU floor is never benchmarked unless the user manually runs on CPU** — `_apply_bench` (planner.py:598-611) and the tps swap in `resolve_translate` (planner.py:344-352) only demote a GPU plan when BOTH the GPU and the CPU entries exist in the cache. `measure_rtf`/`measure_tps`/`measure_rtf_tts` run only on the plan that actually loaded (asr_engine.py:66, translate_engine.py:83, tts_engine.py:145). On a fresh machine the CPU floor is never measured, so a slow GPU rung is never moved — the design's claim that 'device/tier placement adapts freely from measured RTF' is not true of the code it says it relies on. And on the M4 case, CPU would be slower anyway, so 'adapt' has no target.
  *Suggestion:* Either drop the claim from the design, or add an explicit one-time CPU-floor measurement (opt-in, background, after the first session) so `_apply_bench` has something to compare; and define what happens when both tiers are ≥ 1 (see the 'cannot keep up' finding).

- **[medium] Re-download suggestion has no phrasing, no gate, and orphans the old rung on disk** — The design says quant is a download-time recommendation but never specifies the UX when the recommendation changes for a model the user already has: who prompts, with what copy, whether it counts against a metered connection, and what happens to the previous file. `deleteModel(spec.downloadId, chosenVariant?.repo)` (NativeModelManagementSection.tsx:~400) targets the CHOSEN rung only, so after switching to q4_k_m the q8_0 GGUF (up to 8 GB for hy-mt2-7b) stays in the HF cache with no UI to remove it. The consent gate is fine (`hasAcceptedLicense` is keyed by downloadId, line 244, so no re-prompt), but the cross-rung disk story is missing.
  *Suggestion:* Specify: never auto-download; show a passive 'A smaller rung is now recommended for this GPU (saves X GB, ~Y% faster)' line on the card with a single 'Switch' action that downloads the new rung, then offers to delete the old one; list cached-but-unchosen rungs in the picker with a size and a delete affordance.

- **[medium] Probe outcome and subprocess output never reach the Logs panel a bug report is pasted from** — Diagnostics policy: renderer-side failures go through reportError/reportWarning; sidecar contexts keep their channel and the renderer-side caller reports. Today the sidecar's stderr (including any GGML abort text) goes to Electron main's console (electron/native-host-manager.js:130) and never to LogsPanel; `hardware_info_result` carries nativeVersion/lane/preferredDevice (nativeProtocol.ts:53-60) but the connect-time `local.native.hardware` event (LocalNativeClient.ts:89-96) forwards only os/arch/gpus/backends. Nothing in the design puts per-path fast/slow/unsupported + ratio + child stderr on any wire, so 'why is q4_k_m not recommended on my box' cannot be answered from a pasted log, and a re-probe cannot be requested.
  *Suggestion:* Add `accelProbe: {deviceKey, nativeVersion, driver, results: {pathId: {status, ratio, ms, stderrTail}}}` to `hardware_info_result`; include it in the `local.engine.ready` realtime event nativeModelStore already emits; surface a probe that ended in 'unknown' (timeout/crash) via `reportWarning('NativeModelStore', …, {dedupeKey:'accel.probe'})`; add a 'Re-run hardware probe' action that calls probe(force=True) + the new probe with force.

- **[low] CPU probe as specified answers 'fast' for every type, so it can never skip a rung** — ggml-cpu registers a `vec_dot` for every quantised type in `type_traits_cpu` (generic C with SIMD `#ifdef`s inside), so `ggml_get_type_traits_cpu(type)->vec_dot != NULL` is always true; the discriminating information is only the CPU-variant feature bits, and the mapping from feature bits to 'this type is fast' (dotprod → Q4_K/Q8_0 ARM fast paths, i8mm → Q4_0 gemm, AVX-VNNI → Q8) is a table someone must hand-maintain against each ggml pin — exactly the kind of drift the exact-text patch specs exist to catch. Meanwhile CPU is bandwidth-bound and `_tc_pick_quant` already picks the smallest rung on CPU, so the probe's CPU answer changes nothing.
  *Suggestion:* Drop the CPU half of `sk_probe_accel` for the first cut (CPU = smallest rung, as today) or time it like the GPU paths; if the feature-bit table stays, pin it with a test that fails when the ggml pin bumps.

- **[low] 'Slow' threshold and reference path are unspecified but drive a user-facing re-download** — The design says the timed probe is 'compared against the generic reference path on the same device' and yields fast|slow|unsupported, but does not say what the reference is for each path (for Vulkan Q4_K the fallback is dequant + f16 matmul; for Metal bf16 it is f16), what ratio boundary separates fast from slow, or how run-to-run noise (1.2-4.3x spread measured on GB10 when oversubscribed) is handled. A rung flipping between fast and slow across reboots would flip the recommendation and therefore the card's status repo (see the first finding) — precisely the flapping `_quant_budget_bytes` was designed to avoid.
  *Suggestion:* Pin the reference and threshold per path id in the closed vocabulary (e.g. ratio ≥ 1.5 = fast, ≤ 0.9 = slow, else 'neutral' which does not skip), require the probe to run at the 12-thread knee with the GPU otherwise idle, and treat a neutral/unknown result as 'do not skip'.


## Round 2 — written spec, first version


### Lens: quality (19)

- **[high] `model.family` does not exist on ASR or translate cards; the op-table key for translate is undefined** (`§3.3 `_deployment_available` — "`model.family` exists on every card (`_tc_row`'s arch, `_llm_translate_row`'s family, `_tts_gguf_row`'s family)"`) — Only `TtsModel` has a `family` field (catalog.py:615). `AsrModel` is `class AsrModel(_ModelBase): pass` (catalog.py:43) and `_tc_row` never stores its `base` argument — there is no arch on the card. `_llm_translate_row`'s `family` argument is stored as `TranslateModel.prompt_family` (catalog.py:536) and selects a prompt strategy, not a graph: it is `"qwen"` for qwen2.5-0.5b, qwen3-0.6b, qwen3.5-*, AND eurollm-1.7b (catalog.py:545-569) — four different llama.cpp architectures with different op sets (Qwen3.5 in particular is not a plain transformer). `cov = machine_op_coverage(..., model.family)` therefore raises AttributeError on two of three card kinds and, even if it read `prompt_family`, would look up a table under a key that is not a graph family.
  *Fix:* Add `graph_family: str = ""` to `_ModelBase` (the GGUF `general.architecture` / transcribe arch / audio.cpp family); populate it in `_tc_row` (new `arch` param), `_llm_translate_row` (new `arch` param, e.g. `qwen2`, `qwen3`, `qwen35`, `llama`, `gemma3`, `hunyuan`), and `_tts_gguf_row` (= `family`); key `sk_ops.cpp` and `sk_device_supports_ops(stage, family)` by that value and say so in §3.2. List the translate families the tables must cover.

- **[high] `ggml_backend_metal_supports_family` is not callable from `libsokuji_native` under `GGML_BACKEND_DL=ON`** (`§3.1 Metal source — "`ggml_backend_metal_supports_family(backend, family)` (public in `ggml-metal.h`)"`) — `ggml_options.cmake:93` forces `GGML_BACKEND_DL ON`, so the Metal backend is a dlopen'd module and `libsokuji_native` links only `ggml` (base) — exactly the reason §1.1(3) gives for rejecting `ggml_get_type_traits_cpu`. `ggml_backend_metal_supports_family` is defined inside the Metal module (ggml-metal.cpp:628) and the Metal registration's `get_proc_address` returns only `ggml_backend_get_features` (ggml-metal.cpp:871-879, checked in the ggml tree under `~/.cache/sokuji-native-tests/audiocpp-official-src/external/ggml`; re-confirm on v0.22.0). Calling it directly will not link on the Metal lane.
  *Fix:* State one reachable route: (a) derive `SK_FEAT_SIMDGROUP_REDUCTION` from `ggml_backend_dev_supports_op(dev, NORM[f32])` and `SK_FEAT_BF16` from `supports_op(MUL_MAT[bf16])` on the Metal device — the same predicate §3.2 already uses — or (b) link `Metal.framework` into `libsokuji_native` on the metal lane and call `[MTLDevice supportsFamily:MTLGPUFamilyApple7/Apple6]` on `MTLCreateSystemDefaultDevice()`. Drop the "backend initialised lazily and kept for the process" paragraph if (a).

- **[high] Linking `Vulkan::Vulkan` into `libsokuji_native` contradicts §6 "loader absent → known = 0" and the wheel's dependency policy** (`§3.1 Vulkan source — "`sokuji_native` links `Vulkan::Vulkan` when `SOKUJI_GPU_RESOLVED` is `vulkan`" vs §6 "Vulkan loader absent. `known = 0`"`) — A DT_NEEDED / import on `libvulkan.so.1` / `vulkan-1.dll` makes `import sokuji_native` fail outright on a box without the loader — native becomes absent, not `known = 0`, and premise 5 ("an absent profile changes nothing") is violated because the whole wheel is gone. Today the loader is a dependency of the dlopen'd `ggml-vulkan` module only: `native/ci/check_linux_deps.py:11-14` allows `libvulkan.so.1` precisely because "ggml skips a module it cannot dlopen". The spec's own §6 line assumes graceful failure that a hard link cannot give.
  *Fix:* Rewrite §3.1: build the enumerator against Vulkan-Headers only (`VK_NO_PROTOTYPES`), `dlopen`/`LoadLibrary` the loader at profile time, resolve `vkGetInstanceProcAddr`, and set `known = 0` when the loader is missing; add that `libsokuji_native` must NOT gain a `libvulkan` DT_NEEDED and extend `check_linux_deps.py` to assert it.

- **[high] The `bf16` feature gate refuses rungs ggml can execute and duplicates op coverage with a different answer** (`§3.3 `_deployment_available` — `if d.compute_type == "bf16" and "bf16" not in dev.features: return False`; §3.1 `SK_FEAT_BF16 /* Vulkan: VK_KHR_shader_bfloat16 */``) — On Vulkan, ggml's `supports_op` accepts `MUL_MAT` with a BF16 `src0` unconditionally (ggml-vulkan.cpp:15782 in the inspected tree); the scalar `matmul_bf16` pipeline is created in the no-coopmat branch (ggml-vulkan.cpp:4290) and `VK_KHR_shader_bfloat16` only gates the coopmat bf16 variants (`coopmat_bf16_support`). So a Vulkan device without the extension runs bf16 rungs (the bf16 rungs validated on GB10/Vulkan per CLAUDE.md are the evidence), yet this branch refuses them — contradicting §1 ("except that a plan the device cannot execute") and premise 3 ("ggml chooses the kernel path at runtime"). On Metal the same question is already answered by `supports_op` (bf16 mul_mat refused without `has_bfloat`), so the branch is redundant there.
  *Fix:* Delete the `bf16` branch from `_deployment_available` and the §4 test "`bf16` rung without the feature → False"; let the family op table carry `MUL_MAT[bf16]` and let `supports_op` decide. Keep `SK_FEAT_BF16` as a reported fact only (say so in §3.1).

- **[high] Op coverage is computed per family but the gate is per rung: one unsupported dtype fails every rung** (`§3.2 `sk_device_supports_ops(index, stage, family)` / "hold every `(op, dtype)` the family's graph uses" vs §3.3 `_deployment_available(model, d, ...)` returning `cov.all_supported``) — The table for a family must list `MUL_MAT[q4_K]`, `MUL_MAT[q6_K]`, `MUL_MAT[q8_0]`, `MUL_MAT[bf16]`, `MUL_MAT[f16]` … because every rung is the same family. `all_supported` is then false for ALL rungs whenever any one dtype is unsupported (e.g. a device that refuses bf16 mul_mat would lose its q8_0 row too). The signature has no rung/dtype parameter, so the planner cannot ask the right question.
  *Fix:* Either add `const char *compute_type` to `sk_device_supports_ops` and tag each table entry with the rung dtype it belongs to (`"*"` for rung-independent ops), or keep the family-wide query but have `_deployment_available` evaluate only the entries whose weight dtype matches `d.compute_type` (return per-entry results; drop `all_supported` from the decision). State which, and adjust §3.4's `opCoverage` key (`"stage/family"` → `"stage/family/compute_type"` if the former).

- **[medium] "Unsupported op ⇒ cannot run" holds only for engines that bypass `ggml_backend_sched`** (`§3.2 — "exactly the check ggml's scheduler makes before it would fall back or abort"; §3.3 `_deployment_available` refusing the tier on `not cov.all_supported``) — When a graph goes through `ggml_backend_sched`, an op the GPU backend does not support is placed on the CPU backend — the plan runs, degraded. llama.cpp (translate) schedules this way; audio.cpp does not (no `ggml_backend_sched` use in its `src/`, 40 direct `ggml_backend_graph_compute` calls in the inspected v0.7 tree), which is why the paravirtual Metal box aborted on NORM at execution. Refusing the GPU tier for `translate`/`asr` on a missing op withholds a runnable plan, contradicting premise 1 ("tiers say what CAN run").
  *Fix:* Make the refusal rule per stage and write it in §3.3: `tts` (audio.cpp, no scheduler) → refuse on any unsupported op; `asr`/`translate` → refuse only if the engine is verified to run without a scheduler, otherwise keep the tier and surface the unsupported ops as a warning only. Add a line in §2 recording which of the three engines route through `ggml_backend_sched` (verify transcribe.cpp).

- **[medium] `SK_ERR_INVALID` does not exist in the C ABI** (`§3.2 comment "Unknown (stage, family) → SK_ERR_INVALID" and §4 "bad index → `SK_ERR_INVALID`", "`SK_ERR_INVALID` for an unknown pair"`) — `native/include/sokuji_native.h:46-52` defines `SK_OK`, `SK_ERR_INVALID_ARGUMENT`, `SK_ERR_NOT_INITIALISED`, `SK_ERR_BACKEND`, `SK_ERR_NOT_FOUND`, `SK_ERR_CANCELLED`, `SK_ERR_INTERNAL`. The existing sibling `sk_device_free_mem` returns `SK_ERR_INVALID_ARGUMENT` for a bad index (sk_common.cpp:253).
  *Fix:* Replace every `SK_ERR_INVALID` with `SK_ERR_INVALID_ARGUMENT` (or `SK_ERR_NOT_FOUND` for the unknown (stage, family) pair — pick one and use it in both §3.2 and §4).

- **[medium] Lazy op-coverage computation contradicts "planner receives a dict and never touches disk"; `_deployment_available` has no way to receive it** (`§3.3 — "computed lazily, on first use by the planner" vs "`machine_op_coverage` is the planner-side read of the cache, injected the way `bench`/`cache` already are (planner stays pure: it receives a `dict`…)"; the `_deployment_available(model, d, machine)` signature`) — If the planner receives a pre-filled `dict`, nothing computes a missing entry on "first use by the planner" — `cov is None → True` means an uncomputed family is silently passed forever. If instead the planner calls `accel.op_coverage` (native + disk), it is no longer pure and the characterisation suite's `bench_load` pinning pattern does not cover it. The pseudo-code also calls `machine_op_coverage` as a free function while the signature carries no such parameter, and `resolve_deployments`/`select_variant`/`_tc_pick_quant`/`_llamacpp_variant_row` (the four call sites) currently take only `bench` / `est_bytes` / `format_ready`.
  *Fix:* Pick the injected-callable pattern already used for `est_bytes`/`format_ready`: add `op_coverage: Callable[[int, str, str], OpCoverage | None]` (keyword-only) to `_deployment_available` and thread it through the four public entry points; accel supplies the caching + native-calling implementation for `resolve_*`, and a read-only implementation (`cache.get`) for `_h_hardware_info`/`_h_models_catalog`. Remove "planner-side read of the cache" and "receives a dict".

- **[medium] `_h_models_catalog` `tiers[].available` and `_engine_identity` still gate on `_tier_available`, so the catalog advertises a tier `resolve_*` refuses** (`§3.3 — "called from the four places that gate a deployment today"; "the first catalog call therefore answers with whatever is cached"`) — `accel._h_models_catalog` (accel.py:779) computes `available` per tier with `_tier_available`, and `_engine_identity` (accel.py:716) picks `preferredDevice` with it; neither is in the four listed call sites, so after this spec the renderer's tier chips/hardware gating and `preferredDevice` keep saying GPU is available while `resolve_*` routes to the cpu row. The sentence about the catalog "answering with whatever is cached" presumes a switch the spec never lists.
  *Fix:* Add both to the call-site list and state the semantics: `available = _deployment_available(mdl, d, m, op_coverage=read_only)`; with no cached coverage the tier stays `available: true` (premise 5), and `preferredDevice` uses the same rule.

- **[medium] "Three most recent generations" has no ordering source in the cache file** (`§3.3 Generation — "entries whose generation is not among the three most recent seen in the file are pruned"`) — Generations are 12-hex blake2s prefixes; the file is a flat `{key: value}` dict. Nothing records when a generation was first or last seen, so "most recent" is undefined and an implementer will either keep an arbitrary three or add a structure the spec did not describe.
  *Fix:* Specify the file layout: `{"generations": [gen, ...]  # first-seen order, capped at 3 on save, "entries": {key: value}}`; `bench_load` on a legacy flat file returns `{}` (all pre-generation keys are already unreadable under the new key prefix — say that explicitly too).

- **[medium] Metal `driver_version` via `sw_vers -buildVersion` is a subprocess, which §1 forbids** (`§3.1 `driver_version[64]` — "Metal: macOS build (sw_vers -buildVersion)" vs §1 "in-process, in milliseconds, with no timing and no subprocess"`) — `sw_vers` is an external binary; spawning it from the native library contradicts the stated production constraint and adds a failure mode (sandbox, PATH).
  *Fix:* Use `sysctlbyname("kern.osversion")` (returns the build string, e.g. `24F74`) or `NSProcessInfo.operatingSystemVersionString`; write that in §3.1.

- **[low] Names used but never defined: `_stage_of`, `_device_for_tier`, and two names for the coverage reader** (`§3.3 `_deployment_available` pseudo-code`) — `_stage_of(d.backend)` is never defined; the backend names are `native_asr`, `native_asr_stream`, `native_translate`, `native_tts` (catalog.py:17) and the mapping — in particular `native_asr_stream` → `asr` — is not stated. `_device_for_tier` exists only as a trailing comment. The coverage reader is `accel.op_coverage(machine, device_index, stage, family)` in one paragraph and `machine_op_coverage(machine, dev.index, …)` in the code.
  *Fix:* Add to §3.3: `_STAGE_OF_BACKEND = {"native_asr": "asr", "native_asr_stream": "asr", "native_translate": "translate", "native_tts": "tts"}`; `_device_for_tier(machine, tier)` = first `DeviceProfile` whose `kind` is `TIER_DEVICE[tier]`; use a single name for the coverage callable.

- **[low] `"ops:"` cache key and value shape are unspecified** (`§3.3 Op coverage cache — "stores the result in the same file under namespace `"ops:"`"`) — `_bench_key` takes `(fingerprint, model_id, backend, device, compute_type)` (planner.py:183) — none of which is (device_index, stage, family). The stored value must also carry the unsupported op names that §3.4 reports, not just a bool.
  *Fix:* Define `key = f"{machine.generation}|ops:{device_index}:{stage}:{family}"` and value `{"allSupported": bool, "unsupported": ["NORM[f32]", …]}`.

- **[low] Wire `devices[].name` has no source field in `DeviceProfile`** (`§3.4 `devices?: { index; kind; name; description; … }` vs §3.3 `DeviceProfile` (no `name`)`) — `_h_hardware_info` is said to build `devices[]` from `machine.devices`, but `DeviceProfile` carries `description` only; `name` ("Vulkan0", "CPU") lives on `sk_device`.
  *Fix:* Add `name: str` to `DeviceProfile` (copied from the matching `sk_devices()` entry in `native.device_profiles()`), or drop `name` from the wire type.

- **[low] `test_ops_coverage` skip semantics and the "recorded dump" fallback are underspecified** (`§3.2 — "Families without a cached model skip with return code 77"; "validated by a recorded dump instead (the dump, its pin versions and the command that produced it live beside the table)"`) — It is one CTest executable: with whisper-tiny present and supertonic absent it is unclear whether the run is a pass, a skip (77), or a fail. The dump fallback names no file format, location, or producer command, so it cannot be implemented or reviewed as written.
  *Fix:* Rule: rc 77 only when no family's model is present; otherwise assert the present families and print `SKIPPED: <family>` per absent one. For dumps: `native/src/ops/<stage>-<family>.ops` (one `OP[src0,src1]` per line), produced by `sk_dump_ops` (a `--dump-ops` flag on the existing test binary), with the engine pin recorded on line 1.

- **[low] `hardware_info { force: true }` drops `"ops:"` for "the previous generation", which is a no-op when the generation is unchanged** (`§3.4 — "`hardware_info` accepts `force: true`, which calls `probe(force=True)` and drops the `"ops:"` entries for the previous generation"`) — Entries of a previous generation are already unreadable (key prefix) and pruned by the three-generation rule; if re-probing yields the same generation nothing is dropped, so the action re-answers nothing. What a user re-running the probe expects (re-detect hardware, re-answer coverage) is not what is specified.
  *Fix:* Either state "force re-probes and drops the `ops:` entries of the CURRENT generation so coverage is re-answered" or delete the drop clause.

- **[low] "Once per session" warning cannot be delivered by `dedupeKey`** (`§3.4 — "reported through `reportWarning('NativeModelStore', …, { dedupeKey: 'native.ops.unsupported' })` once per session"`) — Per the project's diagnostics policy, `dedupeKey` throttles the panel on a 5 s window; it does not give once-per-session semantics.
  *Fix:* Keep a `reportedUnsupportedOps: Set<string>` (key `"stage/family"`) on `nativeModelStore` and report only on first insertion; keep `dedupeKey` as the burst guard.

- **[low] §7 cites a file that is not in the repository** (`§7 Decisions log — "findings in `esl-b2/design-groundwork.json`"`) — `find . -name design-groundwork.json` (excluding node_modules) returns nothing on this checkout; the adversarial-review findings the spec relies on for premise 3 are unreachable to a reader.
  *Fix:* Commit the findings under `docs/superpowers/notes/2026-09-04-device-profile-review-findings.md` and cite that path, or remove the reference.

- **[low] The Engine-page "Re-run hardware probe" action is a second, unrelated deliverable** (`§3.4 — "the Engine page gets a "Re-run hardware probe" action wired to it, next to the status line … added to all 30 locales"; §4 renderer bullet`) — The profile is computed on every sidecar start and keyed by (hardware, native version, driver); nothing in §1's three goals needs a user-triggered re-probe. It brings a new protocol field, a UI control, 30 locale strings and renderer tests into a native-ABI/planner spec, with no stated user scenario.
  *Fix:* Move the UI action and its locale strings to a follow-up (or spec B's Engine-page work); keep `hardware_info { force }` only if a test needs it, and say so.


### Lens: technical (15)

- **[high] `sk_device_profile` is both a typedef name and a function name — will not compile** (`§3.1 code block: `typedef struct sk_device_profile {...} sk_device_profile;` and `SK_API sk_status sk_device_profile(int32_t index, sk_device_profile *out);``) — In C (and C++), typedef names and function names share the ordinary-identifier namespace; declaring a typedef `sk_device_profile` and then a function `sk_device_profile` in the same scope is a redeclaration error (clang: "redefinition of 'sk_device_profile' as different kind of symbol"). The existing header avoids this everywhere: struct `sk_device` vs functions `sk_devices`/`sk_device_free_mem` (sokuji_native.h:88-102).
  *Fix:* Rename one side, matching the header's existing pattern: keep the struct `sk_device_profile` and name the accessor `sk_device_profile_get(int32_t index, sk_device_profile *out)` (or `sk_device_get_profile`). Mirror the name in `_ffi.py` and the pytest round-trip in §4.

- **[high] Vulkan `supports_op` is not "a question": first call fully initialises the device, compiles the pipeline set, and can throw** (`§3.2 "Nothing is allocated on the device and nothing runs, so a GGML_ABORT cannot occur"; §3.1 "Cost: ... Nothing executes on the device"; §3.3 "computed lazily, on first use by the planner"`) — ggml v0.22.0 `ggml_backend_vk_device_supports_op` (src/ggml-vulkan/ggml-vulkan.cpp:18051-18053) begins with `ggml_vk_get_device(ctx->device)`. On first call that creates the `VkDevice` (`physical_device.createDevice`, relative line 784 of the function) and runs `ggml_vk_load_shaders(device)` (relative 883) — the full pipeline compile, seconds on a cold driver cache — and can `throw std::runtime_error("Unsupported device")` (relative 652) / `"Device not found"` with no catch on the supports_op path, i.e. a C++ exception escaping the `sk_*` C ABI. Nothing today triggers this: `sk_init`/`sk_devices` only reach `ggml_backend_vk_get_device_memory`, which enumerates physical devices (ggml-vulkan.cpp:17908). So the profile call would become the first full Vulkan device init in the sidecar, moved to the planner's first `resolve_*` rather than to a known place.
  *Fix:* State in §3.1/§3.2 that on Vulkan `sk_device_supports_ops` performs full backend device initialisation (VkDevice + pipeline set) and budget it accordingly; wrap the ggml calls in try/catch and return `SK_ERR_BACKEND` / `known = 0` on exception; and run the coverage query in `probe()` (or in `sk_init` behind an option), not lazily inside the planner's first resolve, so the multi-second cost lands where the ready transition already waits.

- **[high] Linking `Vulkan::Vulkan` into `sokuji_native` does not build in CI lanes and makes the loader a hard dependency** (`§3.1 Vulkan bullet: "`sokuji_native` links `Vulkan::Vulkan` when `SOKUJI_GPU_RESOLVED` is `vulkan` (the `find_package` already runs). ... on a box without a Vulkan ICD the enumeration fails and `known = 0`"; §6 "Vulkan loader absent → known = 0"`) — (a) `find_package(Vulkan QUIET)` at ggml_options.cmake:16 runs only inside the `SOKUJI_GPU STREQUAL "auto"` branch; CI configures with an explicit lane (`native/ci/build.sh:14` `-DSOKUJI_GPU="$LANE"`, `build.ps1:14`), so no `Vulkan::Vulkan` imported target exists in native's directory scope. ggml-vulkan's own `find_package(Vulkan COMPONENTS glslc REQUIRED)` (ggml src/ggml-vulkan/CMakeLists.txt:9) is directory-scoped to the FetchContent subtree and not visible to `native/CMakeLists.txt`, so `target_link_libraries(sokuji_native PRIVATE Vulkan::Vulkan)` degrades to `-lVulkan::Vulkan` and fails to link. (b) Once it does resolve, `libsokuji_native.so`/`sokuji_native.dll` gains a DT_NEEDED/import on `libvulkan.so.1`/`vulkan-1.dll`. Today the only Vulkan link is inside the dlopen'd `libggml-vulkan` module (`GGML_BACKEND_DL ON`, ggml_options.cmake:93), which fails soft in `ggml_backend_load_all_from_path`; after this change a machine without the Vulkan loader cannot load `sokuji_native` at all — the opposite of the documented `known = 0` degradation. The linux wheels' `manylinux_2_35` floor also does not admit `libvulkan.so.1` as an allowed external.
  *Fix:* Call `find_package(Vulkan QUIET)` unconditionally when `SOKUJI_GPU_RESOLVED` is `vulkan` (in native's scope), and do not link the loader: resolve `vkGetInstanceProcAddr` at runtime via `dlopen("libvulkan.so.1")`/`LoadLibrary("vulkan-1.dll")`/`libvulkan.dylib` (volk-style, `Vulkan::Headers` only) so absence really yields `known = 0`; or perform the enumeration from inside the ggml-vulkan module boundary.

- **[high] Per-family op tables cannot express the weight type, so `sk_device_supports_ops(index, stage, family)` cannot answer the per-rung question the planner asks** (`§3.2 `sk_device_supports_ops(int32_t index, const char *stage, const char *family, ...)` and "hold every (op, dtype) the family's graph uses"; §3.3 `_deployment_available` calling `machine_op_coverage(machine, dev.index, _stage_of(d.backend), model.family)``) — The src0 type of every weight MUL_MAT is the rung (`_tc_row` ladders F16/Q8_0/Q6_K/Q5_K_M/Q4_K_M, catalog.py:68-86; TTS and LLM ladders q8_0/bf16/q4_k_m/...). A table keyed only by family must either list every rung's type — then any device that refuses one rung's type fails `all_supported` for every rung of the family — or list none, in which case the check is blind to weight dtype, which is the one axis (bf16) §3.3 explicitly wants to gate. The cache key `(generation, device, stage, family)` has the same gap.
  *Fix:* Add a fourth argument (the deployment's `compute_type`, mapped to a `ggml_type` for the weight tensors) to `sk_device_supports_ops`, use a `WEIGHT` placeholder in the static tables that the implementation substitutes per call, and key the `"ops:"` cache and `machine_op_coverage` by `(generation, device, stage, family, compute_type)`.

- **[high] The `bf16` feature rule refuses rungs ggml runs: Vulkan accepts BF16 MUL_MAT unconditionally, and Metal's `has_bfloat` is not `supportsFamily(Apple6)`** (`§3.3 `_deployment_available`: `if d.compute_type == "bf16" and "bf16" not in dev.features: return False`; §3.1 `SK_FEAT_BF16` comment ("Vulkan: VK_KHR_shader_bfloat16; Metal: has_bfloat (Apple6 / Metal3)")`) — Vulkan: `ggml_backend_vk_device_supports_op` lists `GGML_TYPE_BF16` among the always-accepted MUL_MAT src0 types (ggml-vulkan.cpp:18138-18140, no feature check; the only bf16 refusal is bf16 x f16 src1 at 18181-18185) — the device extension merely selects a faster kernel, and even that requires `GGML_VULKAN_BFLOAT16_GLSLC_SUPPORT` at build time and no `GGML_VK_DISABLE_BFLOAT16` (6251-6254). A card without `VK_KHR_shader_bfloat16` would lose bf16 rungs it executes today. Metal: ggml's `has_bfloat` = `Metal3_GGML || Apple6`, then forced false by `GGML_METAL_BF16_DISABLE` (ggml-metal-device.m:1050-1053) and by the tensor-API dummy-compile failure on Metal4 devices (1160-1168); `supportsFamily(Apple6)` alone can say `bf16` while `ggml_metal_device_supports_op` returns `has_bfloat` = false for the bf16 mul_mv (1503). The spec's own §2 note that Metal `has_bfloat` is "true on every Apple Silicon GPU we ship to" is therefore also not guaranteed.
  *Fix:* Delete the feature-bit rule from `_deployment_available`; let the op check answer it (a `MUL_MAT[bf16]` entry, per the compute_type fix above) because `supports_op` encodes each backend's real rule including build flags and env overrides. Keep the raw VK/Metal bits in the profile for diagnostics only, and say so in §3.1.

- **[medium] `all_supported` as a tier gate is wrong for the two engines that run through `ggml_backend_sched` — only audio.cpp aborts on an unsupported op** (`§3.2 "exactly the check ggml's scheduler makes before it would fall back or abort"; §3.3 `return cov is None or cov.all_supported` applied to every stage; §1 "a plan the device cannot execute is no longer offered"`) — llama.cpp runs every graph through `ggml_backend_sched` with the CPU backend in the list (`ggml_backend_sched_set_eval_callback` at llama-context.cpp:1354; weight placement already consults `ggml_backend_dev_supports_op` per tensor at llama-model.cpp:2057 `select_buft` and falls back to a CPU buffer type) and transcribe.cpp does the same in every arch (`ggml_backend_sched_new` + `ggml_backend_sched_graph_compute`, e.g. src/arch/sensevoice/model.cpp:470/541, cohere/model.cpp:735/788, moss/model.cpp:735). For those, an op the GPU refuses is scheduled on CPU — the plan executes, slower. Only audio.cpp computes single-backend (`ggml_backend_graph_compute(impl.backend, graph)` across src/models/* and src/community_models/*, no sched anywhere), which is where `GGML_ABORT("unsupported op")` (ggml-metal-ops.cpp:203-204) fires. Refusing an entire gpu tier for asr/translate on one unsupported op turns "cannot execute" into "would partly fall back", contradicting premise 1 ("withholding a tier ... only pushes that machine onto a slower path"). (Verified against llama.cpp v0.2.0 and transcribe.cpp 0.2.2 sources on disk; the pinned v0.3.0 / 0.2.3 trees are not present locally, but the sched design predates both.)
  *Fix:* In §3.3 make the rule per stage: `all_supported` gates `native_tts` only; for `native_asr`/`native_translate` the profile records unsupported ops for diagnostics (and spec C's speed signal) but does not withhold the tier. State the audio.cpp single-backend fact as the reason.

- **[medium] `ggml_backend_sched_eval_callback` cannot observe audio.cpp graphs, so `test_ops_coverage` cannot validate the TTS tables it names** (`§3.2 "loads each family that has a cached test model (... moss-tts-nano, supertonic-3 today) on the CPU device with a `ggml_backend_sched_eval_callback` that records every (op, src0->type, src1->type)"`) — The callback type exists (ggml-backend.h:316, set via `ggml_backend_sched_set_eval_callback` at :354) but fires only inside `ggml_backend_sched_graph_compute`. audio.cpp never creates a sched: every family calls `ggml_backend_graph_compute(backend, graph)` directly (src/models/moss/moss_tts_local/backbone.cpp:441/582, depth_transformer.cpp:408, heads.cpp:166, and the same shape in every other model dir). So of the five cached models, the two TTS ones — the only stage where the gate actually prevents an abort — fall into the "recorded dump" escape hatch, which names no mechanism for producing the dump. The llama.cpp recording is also device-shaped: flash-attention auto and norm placement consult `ggml_backend_supports_op` per device (llama-context.cpp:2521, `auto_fa`), so a CPU-recorded set can differ from the GPU graph.
  *Fix:* Name the mechanism: in the test build, hand audio.cpp a recording backend — a thin `ggml_backend_i` wrapper (ggml-backend-impl.h is already on native's include path for audiocpp_compat.h) whose `graph_compute` walks `cgraph->nodes[i]` recording `(op, src[0]->type, src[1]->type)` and forwards to the real CPU backend — and use the same wrapper for all three engines so the table check does not depend on sched. Add a line that llama.cpp is recorded with `flash_attn_type` forced to both settings.

- **[medium] `model.family` does not exist on ASR or translate cards** (`§3.3 "`model.family` exists on every card (`_tc_row`'s arch, `_llm_translate_row`'s family, `_tts_gguf_row`'s family)"`) — `class AsrModel(_ModelBase): pass` (catalog.py:43-44) and `_ModelBase` (23-41) carry no `family`/`arch`; `_tc_row` (68-86) stores nothing about the transcribe architecture. `TranslateModel` has `prompt_family` (466), which is the prompt-strategy selector (`translate_backend.STRATEGIES`), not a llama architecture and not named `family`. Only `TtsModel.family` (615) exists. `_deployment_available` and `machine_op_coverage` would raise `AttributeError` on two of three card types.
  *Fix:* Add `family: str = ""` to `_ModelBase`, populate it in `_tc_row` (transcribe arch, a new parameter) and `_llm_translate_row` (llama arch), and keep `prompt_family` separate; add a catalog invariant test that every card's `family` is non-empty.

- **[medium] Metal `driver_version` via `sw_vers -buildVersion` is a subprocess** (`§3.1 struct comment: `driver_version[64]; /* ... Metal: macOS build (sw_vers -buildVersion) ... */` vs §1 "in-process, in milliseconds, with no timing and no subprocess"`) — `sw_vers` is an external binary; spawning it from the native layer (or from `probe()`) breaks the stated contract and adds tens of ms plus a failure mode on hardened/sandboxed runs.
  *Fix:* Read `kern.osversion` with `sysctlbyname()` in `sk_device_profile_get` (in-process, same string `sw_vers -buildVersion` prints), or on the Python side `platform.mac_ver()` — both are in-process.

- **[medium] UMA rule misquoted: ggml's Vulkan `uma` is `deviceType == INTEGRATED_GPU`, not "every heap DEVICE_LOCAL|HOST_VISIBLE"** (`§3.1 `SK_FEAT_UMA` comment and Vulkan bullet "reads VkPhysicalDeviceMemoryProperties for UMA (the rule ggml applies: every heap is both device-local and host-visible)"`) — ggml v0.22.0 sets `device->uma = device->properties.deviceType == vk::PhysicalDeviceType::eIntegratedGpu` (ggml-vulkan.cpp:6394) and the same for the debug line (7286). No heap-flag rule exists. A discrete card exposing a single DEVICE_LOCAL|HOST_VISIBLE heap (ReBAR configurations) would be marked UMA by the spec's rule while ggml treats it as discrete, and the profile would then be a wrong input to anything spec C derives from it.
  *Fix:* Derive `SK_FEAT_UMA` from `VkPhysicalDeviceProperties.deviceType == VK_PHYSICAL_DEVICE_TYPE_INTEGRATED_GPU` and rewrite the comment; drop the `VkPhysicalDeviceMemoryProperties` read.

- **[low] `SIMDGROUP_REDUCTION` rule omits ggml's `Metal3` alternative** (`§3.1 Metal bullet: "`MTLGPUFamilyApple7` (1007) → `SK_FEAT_SIMDGROUP_REDUCTION`"`) — ggml computes `has_simdgroup_reduction = supportsFamily(Apple7) || supportsFamily(MTLGPUFamilyMetal3_GGML /* 5001 */)` (ggml-metal-device.m:26, 1044-1045). The integer values 1006/1007 are correct, but a device that reports Metal3 without Apple7 would be refused by §3.3's structured paravirtual rule while ggml runs NORM on it.
  *Fix:* Set the bit when either `ggml_backend_metal_supports_family(b, 1007)` or `(b, 5001)` is true, mirroring ggml's expression, and cite both constants.

- **[low] The Vulkan feature bits are raw extension/feature bits, not what ggml enables** (`§3.1 `sk_feature` enum comments and "So the profile records device facts (features, driver, uma, op coverage)"`) — ggml gates each of these further: coopmat needs `GGML_VULKAN_COOPMAT_GLSLC_SUPPORT` at build, the extension, no `GGML_VK_DISABLE_COOPMAT`, and then passes `ggml_vk_khr_cooperative_matrix_support(props, driver_props, arch)` (a driver/arch deny-list, ggml-vulkan.cpp:6158, 6230-6235); coopmat2/integer_dot/bf16 have the same `*_GLSLC_SUPPORT` + env gates (6237-6254); fp16 is `fp16_storage && fp16_compute && !force_disable_f16 && vk12.shaderFloat16` (6443, 6667). A profile that says `coopmat` on a build whose glslc lacked the extension misleads the log reader the profile exists for.
  *Fix:* Either name the fields for what they are (`vk_ext_coopmat`, ...) with a comment that ggml applies further gates, or add the build-time `GGML_VULKAN_*_GLSLC_SUPPORT` defines (known at native build time) to the profile so the pair is readable together.

- **[low] Generation ignores `GGML_METAL_*` env knobs that change Metal paths** (`§3.3 generation hash: `sorted(k=v for k, v in os.environ if k.startswith("GGML_VK_"))`; §6 "`GGML_VK_*` in the generation ... those variables change ggml's paths"`) — `GGML_METAL_BF16_DISABLE` flips `has_bfloat` (ggml-metal-device.m:1052) and `GGML_METAL_DEVICES` changes device emulation (1040), so the same reasoning that puts `GGML_VK_*` in the hash applies on the Metal lane and is missed.
  *Fix:* Hash every `GGML_VK_*` and `GGML_METAL_*` variable (or simply every `GGML_*`).

- **[low] "Three most recent generations" is not derivable from the flat bench file** (`§3.3 "entries whose generation is not among the three most recent seen in the file are pruned"`) — `accel-bench.json` is a flat `{key: float}` written by `bench_save` (accel.py:500-508); a generation is a 12-hex blake2s prefix with no ordering, so "most recent" has no source once the keys are prefixed.
  *Fix:* Change the file shape to `{"generations": [{"id": ..., "first_seen": ts}], "entries": {...}}` (or store a per-entry timestamp) and prune on that list; state the migration for the existing flat file (treat it as generation-less and drop it).

- **[low] Citation and constant nits** (`§2 "`sk_devices()` (line 233)"; §2 "`test_accel.py:917-928` ... `_fake_native_module(...)`"; §3.2/§4 `SK_ERR_INVALID``) — `sk_devices` is defined at sk_common.cpp:229 (233 is `sk_device_free_mem`'s neighbourhood). `_fake_native_module` starts at test_accel.py:929, outside the cited 917-928 range. The header has no `SK_ERR_INVALID`; the code is `SK_ERR_INVALID_ARGUMENT` (sokuji_native.h:46), which `sk_device_free_mem` already returns for a bad index.
  *Fix:* Correct the two line references and use `SK_ERR_INVALID_ARGUMENT` in §3.2's comment and §4's CTest expectations.


### Lens: tests (11)

- **[high] Generation-prefixed bench keys: planner read side not updated, six existing tests break** (`§3.3 "Generation" paragraph (`accel._measure` keys every entry as f"{machine.generation}|{ns}{_bench_key(...)}") and §4 "sidecar accel" bullet`) — The spec changes only the write side. The read side is in the pure planner: `_resolve_model` (planner.py, `key = _bench_key(machine.fingerprint, ...)`; `if key in cache`) and `resolve_translate`'s `_tps` (`cache.get("tps:" + _bench_key(...))`). If those stay as written, `_apply_bench` demotion and the E6 tps swap never fire again (keys never match) — a silent behaviour change no §4 test catches. If they are updated, the tests that build `cache` with bare `_bench_key` break: test_planner.py:395-396, 408-409, 512-514, 848-849. Either way two accel tests break today: test_accel.py:413 `test_measure_rtf_runs_and_caches` asserts `accel._bench_key(m.fingerprint, ...) in cache` (the key is now `"|whisper-base|..."` since fixtures carry generation ""), and test_accel.py:430 asserts `any(k.startswith("tps:") for k in cache)` (keys now start with `"|tps:"`). §4 lists none of them.
  *Fix:* Add to §3.3: one helper `planner._cache_key(machine, ns, model_id, backend, device, compute_type) -> f"{machine.generation}|{ns}{_bench_key(...)}"` used by `_measure`, `_resolve_model` and `resolve_translate._tps`. Add to §4 sidecar planner: "a bench entry written under generation G is read back by `resolve`/`resolve_translate` under G and ignored under G'" and list test_planner.py:395/408/512/848 and test_accel.py:413/430 as updated to the new key shape.

- **[high] §3.5 compatibility claim is false and contradicts premise 5; no test pins the older-wheel path** (`§3.5 "Compatibility: a sidecar running against a 1.0.x wheel (ABI 1) refuses to load it (existing behaviour)" vs §1.1(5) and §3.3 "() when the wheel is absent or older than ABI 2"`) — The ABI equality check lives inside the wheel (`native/python/sokuji_native/__init__.py:69-95`: contract.json vs `_ffi.SK_ABI_VERSION` vs `sk_abi_version()` — all three ship in the same wheel). `sidecar/sokuji_sidecar/native.py` imports `sokuji_native` and calls `init`; it never reads an ABI or version. A sidecar 0.3.0 against a 1.0.x wheel therefore loads fine, and `native.device_profiles()` fails with AttributeError on the missing `sk_device_profile` symbol → `_safe` → `devices=()` — which is exactly the premise-5 pass-through, not a refusal. §4 has no test for either outcome.
  *Fix:* Rewrite §3.5 to: "a sidecar against a 1.0.x wheel loads it and degrades: `devices=()`, `generation` still computed from `sk_version`/`engine_versions`, every resolve unchanged". Add to §4 sidecar accel: `_fake_native_module` WITHOUT `device_profile`/`device_supports_ops` → `probe().devices == ()`, `generation != ""`, and `resolve_tts` output identical to the same machine with no native; and `generation == ""` only when `sokuji_native` is absent (sys.modules None).

- **[high] Characterisation autouse fixture cannot pin Machine.devices; the real guarantee is the dataclass defaults, and no guard proves no native call happens** (`§4 "characterisation. An autouse fixture pins `Machine.devices=()` and `generation=""`" and §3.5 "Fixtures and the characterisation suite run with `devices=()`"`) — test_characterization.py builds its four Machines directly with keyword args at module import (`accel.Machine(os=..., tc_kinds=..., gpus=...)`, lines 55-80) — frozen dataclass instances, never via `probe()`. A fixture cannot set a field on a frozen instance and patching `Machine.devices` on the class does not change instance `__dict__` values already set at construction. What actually keeps every matrix row unchanged is (a) the field defaults `devices=()` / `generation=""` in §3.3 and (b) `_deployment_available`'s `dev is None → True` branch. That is sufficient for the matrices, but the suite's isolation pins only `_downloaded_quants` and `bench_load`; if the accel `resolve*` wrappers gain an op-coverage step that reads/computes through a different name (`op_coverage`, `native.module`), nothing in the file proves it is not reached.
  *Fix:* Replace the §4 claim with: "the `Machine` field defaults are load-bearing (positional/keyword construction in test_characterization.py, test_planner.py, test_accel.py, test_catalog.py:483, test_platform_filter.py:11 must all stay valid)"; add to the characterisation file an autouse fixture that monkeypatches `accel.op_coverage` (and `native.module`) to `pytest.fail("native reached with devices=()")`, plus one assertion `all(m.devices == () and m.generation == "" for m in _ALL_MACHINES)`.

- **[medium] New probe detectors fall outside the monkeypatch pattern every existing probe test relies on** (`§3.3 "`probe()` fills `devices` from `native.device_profiles()` ... through the same `_safe` policy" and "`probe()` computes generation = blake2s(sk_version | engine_versions | ...)"`) — Every test that calls `probe(force=True)` isolates it by patching exactly `_native_kinds`, `_native_gpus`, `_apple_silicon`, `_installed` (test_accel.py:16-46, 255-286, 354-390, 1002-1010; test_platform_filter). The new profile and generation detectors are not on that list, so on a dev box with the wheel installed those tests would `sk_init` the real library in-process (slow, Vulkan init, module-global `_module` side effect) and on CI silently hit ImportError. `test_probe_degrades_when_detector_throws` (test_accel.py:28) also does not cover the new detectors.
  *Fix:* Name the detectors in §3.3 as module-globals: `_native_profiles() -> tuple[DeviceProfile,...]` and `_native_generation() -> str`, each wrapped by `_safe` in `probe()`. In §4: every existing `probe(force=True)` test gains those two patches (or a shared `_isolate_probe` fixture), and two degrade tests (`_native_profiles` raises → `devices=()`; `_native_generation` raises → `generation=""`).

- **[medium] wire_schema.json not mentioned; strict outbound validation and the TS field-consistency test both fail on the new hardware_info_result fields** (`§3.4 "`hardware_info_result` gains: generation?, devices?" and §4 "renderer. `HardwareInfoResultMsg` type"`) — `sidecar/sokuji_sidecar/wire_schema.json` pins `hardware_info_result` to required [id, os, arch, cpuCores, gpus, backendsInstalled, accelAvailable] + optional [nativeVersion, engineVersions, lane, preferredDevice]; `wire.validate_outbound` raises `WireContractError` on any extra top-level field under `SOKUJI_WIRE_STRICT=1`, which conftest.py sets for the whole sidecar suite. On the renderer side `nativeProtocol.consistency.test.ts` ("every ServerMsg member matches the schema field-for-field") diffs the TS interface's required/optional sets against that file. Adding the fields to `HardwareInfoResultMsg` without the schema fails the vitest; adding them to the handler without the schema fails any strict-mode send.
  *Fix:* §3.4: add `generation` and `devices` to `hardware_info_result.optional` in `wire_schema.json` in the same commit as the TS interface and the handler. §4 renderer: cite `nativeProtocol.consistency.test.ts` as the pin for the type, not a new test.

- **[medium] ABI/version bump sites incomplete: CMake `SK_ABI_VERSION_NUM` and the `test_common.cpp` version assert are missing** (`§3.1 "`SK_ABI_VERSION` goes to 2", §2 (names only `sokuji_native.h:42`, `_ffi.py:6`), §3.5 "Native: ABI 2, version 1.1.0"`) — `native/CMakeLists.txt:120 set(SK_ABI_VERSION_NUM 1)` is a third independent literal and is what `cmake/contract.json.in` (`"abi": @SK_ABI_VERSION_NUM@`) stamps into the wheel; nothing checks it against the header. Left at 1, every `@needs_tree` binding test and the sidecar itself fail at `_load()` with "contract.json ABI 1 != binding ABI 2" — caught, but only after a full five-SKU build. Separately, `native/tests/test_common.cpp:24` asserts `sk_version() == "1.0.2"`; README "Bumping a pin" step 4 names it as the second hard-coded site, and §3.5/§4 do not (nor the "Current native version" lines in native/README.md:420 and CLAUDE.md).
  *Fix:* §3.5 enumerate the exact sites: `sokuji_native.h` `#define SK_ABI_VERSION 2`, `_ffi.py` `SK_ABI_VERSION = 2`, `CMakeLists.txt` `SK_ABI_VERSION_NUM 2` and `project(... VERSION 1.1.0)`, `test_common.cpp` `"1.1.0"`, README/CLAUDE.md version lines. Add a configure-time check in CMakeLists.txt that greps `#define SK_ABI_VERSION` from the header and `message(FATAL_ERROR)`s on mismatch with `SK_ABI_VERSION_NUM`, and list it under §4 native.

- **[medium] Catalog/list_variants/engine_identity still gate on `_tier_available`; the wire keeps offering tiers and rungs the planner now refuses, untested** (`§3.3 "`_deployment_available` ... called from the four places that gate a deployment today" and "the first catalog call therefore answers with whatever is cached"; §1 "a plan the device cannot execute is no longer offered"`) — `_h_models_catalog` computes `tiers[].available` via `_tier_available(d.tier, m, d.backend)` and sets `variants[].supported = True` unconditionally for GGUF cards (accel.py `_h_models_catalog`); `_h_list_variants` and `_engine_identity` (`preferredDevice`) do the same. None of these are among the four named call sites, so a bf16 rung on a device without the feature, or a family with an unsupported op, is still advertised as available/supported (the renderer's `hardwareGated` and the variant dropdown read exactly those fields) while the planner routes it to cpu. The sentence about the catalog "answering with whatever is cached" implies the catalog reads coverage, but no code path or §4 test says so.
  *Fix:* Decide and state it: either (a) `tiers[].available` and `variants[].supported` go through `_deployment_available` using the cached `ops:` entries read-only (miss → available, per premise 5), with §4 tests `_h_models_catalog` marks gpu-vulkan unavailable when the cached coverage for that family is unsupported, and marks a bf16 rung `supported: False` when the profile lacks bf16; or (b) the catalog stays tier-level and only plans change — then add the test that catalog output is identical with and without a known profile and note the UI mismatch as a §6 limitation.

- **[medium] Who computes op coverage is contradictory; no test pins the trigger** (`§3.3 "Op coverage cache" ("computed lazily, on first use by the planner") vs "`machine_op_coverage` is the planner-side read of the cache, injected ... receives a dict and never touches disk"; §4 "`op_coverage` is computed once per key"`) — If the planner receives a dict, a cache miss returns None and `_deployment_available` passes through — nothing ever computes coverage unless the accel `resolve*` wrappers do so before delegating. The spec never says the wrappers do, and §4's "computed once per key" names no caller. This matters for the characterisation suite (a wrapper-side native call must be skipped when `devices=()`) and for the `_h_models_catalog` critical-path promise.
  *Fix:* §3.3: "`accel.resolve/resolve_translate/resolve_tts` call `op_coverage(m, dev.index, stage, model.family)` for the resolved card iff `m.devices` holds a known non-cpu profile, then pass the resulting dict to the planner as `ops=`". §4 sidecar accel: the wrapper calls `native.device_supports_ops` exactly once for (generation, device, stage, family) across two resolves; the wrapper never touches native when `devices=()` or `known=False`.

- **[low] "Three most recent generations" pruning has no recency source in a flat key→value file** (`§3.3 "on save, entries whose generation is not among the three most recent seen in the file are pruned"; §4 "pruning keeps three generations"`) — `accel-bench.json` is a flat dict (`bench_load`/`bench_save`, accel.py); entries carry no timestamp, so "most recent" can only mean JSON insertion order, which `_measure`'s `cache[key] = val` on a possibly-existing key does not preserve. The §4 test cannot assert an undefined ordering.
  *Fix:* Add a `"_generations": [...]` list to the file (append current generation on first save under it, newest last; prune keys whose prefix is not in the last three; `bench_load` strips it before returning to the planner). §4 test: after saves under G1..G4, the file holds only G2..G4 keys and `_generations == [G2, G3, G4]`.

- **[low] `engineInfo` exact-equality test breaks if devices/generation are stored inside it** (`§3.4 "`nativeModelStore` keeps them beside the engine identity it already stores"; §4 renderer "store keeps them"`) — src/stores/nativeModelStore.test.ts:456 does `expect(engineInfo).toEqual({nativeVersion, engineVersions, lane, preferredDevice})`; "beside" is ambiguous between new keys on `engineInfo` (breaks the test) and new store fields (does not).
  *Fix:* State separate store fields (`deviceProfiles`, `profileGeneration`) in §3.4, or list nativeModelStore.test.ts:456 as updated in §4.

- **[low] `force: true` drop of previous-generation `ops:` entries is a behavioural claim with no test** (`§3.4 "`hardware_info` accepts `force: true`, which calls `probe(force=True)` and drops the `"ops:"` entries for the previous generation"; §4 "`force: true` re-probes"`) — §4 pins only the re-probe; the drop (and that a non-force call leaves them) is untested, and the strict wire validator only covers outbound, so nothing pins the inbound `force` field either.
  *Fix:* §4 sidecar accel: after `hardware_info {force:true}` with a changed fake driver string, `bench_load()` holds no `ops:` key under the old generation and still holds the RTF keys; `hardware_info` without `force` returns the cached Machine and leaves the file untouched.


## Round 3 — second draft, finding-by-finding re-check


### Lens: closure (9)

- **[high] New inconsistency: `variants[].supported: False` for a GPU-refused TTS rung makes the rung unpickable and drops the user's pin, contradicting the cpu-row fallback (introduced by the fix to "Catalog/list_variants/engine_identity still gate on `_tier_available`")** (`§3.3 "the three wire producers (`_h_models_catalog`'s `tiers[].available` and `variants[].supported`, …)"; §4 sidecar accel: "marks `gpu-vulkan` unavailable and the rung `supported: False` for a TTS card whose cached coverage is unsupported"`) — `variants[].supported` is a per-rung flag with no tier axis, and the renderer treats it as "cannot load at all": `NativeModelManagementSection.tsx:114/128` no-ops the click and renders `disabled={!v.supported}`, and `nativeModelStore.ts:226` (`deriveVariantRepos`) deletes a persisted pin whose variant has `supported === false`. §3.3 simultaneously promises that a pinned or downloaded TTS rung the GPU refuses "resolve[s] … to its cpu row instead of aborting". Emitting `supported: False` therefore refuses in the UI a rung the planner will run on CPU, and silently drops the pin the planner says wins. Today `supported` is unconditionally True for every GGUF card (accel.py:814-829) precisely because the tier fallback handles capacity.
  *Fix:* Keep `variants[].supported` meaning "loadable on some tier" (unchanged: True for GGUF cards, since the cpu row always passes — §4 pins `all_supported == 1` on CPU). Carry the per-rung GPU refusal on a new optional field (e.g. `variants[].unsupportedTiers: string[]` or `variants[].gpuUnsupported: boolean`) that the renderer can render as a muted note without disabling the option, and reword the §4 test to `tiers[].available == False` for gpu-vulkan plus that new field, not `supported: False`. Add the field to `wire_schema.json`/`nativeProtocol.ts` alongside `devices`.

- **[medium] Partially resolved: "`ggml_backend_sched_eval_callback` cannot observe audio.cpp graphs" — the recording backend cannot be handed to any of the three engines, which take a device or a backend-type + relative index, never a `ggml_backend_t`** (`§3.2 first bullet: "wraps the CPU backend in a thin recording backend … and hands that backend to each engine for one forward pass"`) — None of the engines accept a backend object. `sk_translate.cpp:178-184` passes `ggml_backend_dev_t` through `llama_model_params.devices`; `sk_asr.cpp:120` passes a `ggml_backend_dev_t`; `sk_tts.cpp:386-388` passes a `core::BackendType` plus `backend_relative_index(dev)`, and audio.cpp then calls `ggml_backend_dev_init(device, nullptr)` (framework/core/backend.cpp:157) or `ggml_backend_init_by_type(GGML_BACKEND_DEVICE_TYPE_CPU, …)` (:230) itself. A bare `ggml_backend_i` wrapper is unreachable from all three, so the CTest as written cannot record the TTS graphs — the one stage the gate protects — and the "same wrapper for all three engines" sentence is unimplementable.
  *Fix:* Specify a recording *device*, not a backend: in the test build register (via `ggml_backend_register`) a `ggml_backend_reg` exposing one `ggml_backend_dev_i` of type GPU whose `supports_op` returns true for every op, whose `get_buffer_type`/`supports_buft` return the CPU buffer type, and whose `init_backend` returns the recording backend that forwards `graph_compute` to a real `ggml_backend_cpu_init()`. Register it before `sk_init` so it appears in `sk_devices()` as an ordinary flat index that all three engines already accept (llama's `devices`, transcribe's dev, audio.cpp's relative index). Alternatively keep the sched eval callback for llama.cpp/transcribe.cpp and use the recording device only for audio.cpp — but say which.

- **[medium] Partially resolved: "Vulkan `supports_op` is not a question: first call fully initialises the device" — the cost is now paid before loads that never touch Vulkan (override="cpu", cpu-row plans, second GPU)** (`§3.2 "placed where the same initialisation would be paid anyway — in the `accel.resolve_*` wrappers immediately before the load that follows them"; §3.3 "on each non-cpu device whose profile is `known`"; §6 "paid in the resolve wrapper right before the load that would pay it anyway"`) — The wrappers are called with `override=device or "auto"` from `asr_engine.py:68/517/531`, `translate_engine.py:79`, `tts_engine.py:129`, so `override == "cpu"` reaches them; §3.3 has the wrapper compute coverage for every listed rung on every known non-cpu device *before* the planner picks a tier. An explicit-CPU load, a model whose budget forces the cpu row, and a card whose GPU tier the planner never considers all now pay VkDevice creation plus the full `ggml_vk_load_shaders` compile (seconds cold) that the load itself would not have paid. On an iGPU+dGPU box both Vulkan devices are initialised although `native.device_for` only ever loads on the first. The "paid anyway" claim holds only for the auto-on-GPU path.
  *Fix:* Make the injected callable compute lazily — `_deployment_available` asks only for gpu tiers it is actually gating, so coverage is computed when a GPU deployment is being considered, never for cpu-only resolutions — and have the wrapper skip coverage entirely when `override == "cpu"`. Restrict the query to the device `native.device_for(TIER_DEVICE[tier])` would load on (the first of that kind), matching the multi-GPU rule §5 leaves unchanged. Rewrite the §3.2/§6 cost sentences accordingly and add a §4 test: `resolve_tts(..., override="cpu")` on a machine with a known Vulkan profile never calls `native.device_supports_ops`.

- **[medium] Description-string device matching attributes the wrong driver when two ICDs expose the same GPU — the exact case the generation exists to detect** (`§3.1 Vulkan bullet: "ggml's device `i` is matched to a physical device by description string, and among identical descriptions by order"; §6 "Identical GPUs … only differs if their drivers differ"`) — ggml's own enumeration (`ggml_vk_instance_init`, ggml-vulkan.cpp:7494-7548, v0.22.0) dedups physical devices with the same `deviceUUID` and keeps one by a driverID priority table (RADV 1 > AMDVLK 2 > AMD proprietary 3; Mesa Intel > Intel proprietary; NVIDIA proprietary > NVK; Dozen 100) and also honours `GGML_VK_VISIBLE_DEVICES` (:7444). Both ICDs for one card report the identical `deviceName`, so "among identical descriptions by order" selects whichever ICD the raw loader enumeration lists first — frequently the one ggml discarded. `driver_name`/`driver_version`/`device_uuid` (identical UUID, different driver) then describe a driver that never runs a kernel, and a RADV↔AMDVLK switch, the motivating example in §1(2), leaves the generation unchanged. §6 only covers two physically distinct identical cards.
  *Fix:* Replicate ggml's selection in the enumerator: group physical devices by `deviceUUID`, apply the same driverID priority table (pinned per ggml bump the way `native/patches/*.json` are, with a comment naming ggml-vulkan.cpp's line), apply `GGML_VK_VISIBLE_DEVICES`, then match ggml's index `i` positionally to the surviving list; fall back to description matching only when the counts differ. Add the dual-ICD case to §6 and a CTest that feeds two fake `(uuid, driverID)` pairs through the selection helper.

- **[medium] New inconsistency: premise 6, the §3.1 enum comment and §5 say feature bits never gate, but §3.3's structured paravirtual rule gates `gpu-metal` on `mtl_simdgroup_reduction`** (`§1.1(6) "the raw feature bits in the profile are reported for diagnostics and never gate anything"; §3.1 Metal enum comment "Diagnostics only; §3.2 is the gate"; §5 "any use of the raw feature bits beyond diagnostics" (out of scope); versus §3.3 "Paravirtual (R36). `_tier_available`'s Metal branch becomes: if the Metal device's profile is known, refuse when `mtl_simdgroup_reduction` is absent"`) — The fix to "The `bf16` feature gate refuses rungs ggml can execute" removed the bf16 bit rule and generalised the text to "no feature-bit rule", but the R36 structured rule that the same draft adds is a feature-bit gate on the whole Metal tier. The Metal bits are derived from `supports_op` (§3.1), so the decision is defensible, but three sections now contradict the one that implements it, and an implementer following premise 6 would drop the R36 rule.
  *Fix:* Reword premise 6 and the §3.1 Metal comment: "the Vulkan bits are raw and never gate; the two Metal bits are `supports_op` answers, and `SK_FEAT_MTL_SIMDGROUP_REDUCTION` is the single bit that gates — tier-level, in `_tier_available`, because NORM is in every family's graph (R36)". Change §5 to "any use of the raw *Vulkan* feature bits beyond diagnostics".

- **[low] `_native_generation()` is specified as a zero-argument detector but its hash needs the profiles; and the two statements of when `generation == ""` disagree** (`§3.3 "`_native_generation() -> str`" and the generation formula (`… for d in devices sorted by index …`); §3.3 `Machine.generation` comment "\"\" only when sokuji_native is absent" vs §4 "`_native_generation` raising → `generation=\"\"`"`) — The formula hashes `(kind, device_uuid, driver_name, driver_version)` of every profile, but the detector takes no input and `_native_profiles` is a separate `_safe`-wrapped detector; either `_native_generation` re-runs the profile enumeration (a second VkInstance, and a different answer if one of the two calls fails) or the spec is missing its parameter. Separately, §3.3's own comment restricts `""` to the absent-wheel case while §4 makes any raise in the detector produce `""` — under which every bench key is written with an empty prefix and read back across real generation changes.
  *Fix:* Define `_native_generation(devices: tuple[DeviceProfile, ...]) -> str` (or split it into a `_native_versions() -> (sk_version, engine_versions)` detector and compute the hash in `probe()` from that result plus the `_native_profiles` result), and make the field comment match §4: `""` when the wheel is absent or the version detector fails; a profiles-only failure still yields a version-keyed generation.

- **[low] `accel.op_coverage` behaviour on `SK_ERR_NOT_FOUND` and `SK_ERR_INVALID_ARGUMENT` is unspecified, and NOT_FOUND will be the common case for asr/translate at first release** (`§3.3 "Op coverage cache" (only `SK_ERR_BACKEND … is not cached and returns None`); §3.2 status comment`) — §3.2 requires tables for the transcribe.cpp architectures behind 66 ASR cards and six llama.cpp architectures, but only five test models are cached and the dump route needs a developer to load every family once; tables will land incrementally, so `sk_device_supports_ops` answering `SK_ERR_NOT_FOUND` is the expected answer for most asr/translate cards on day one. The spec says nothing about whether the wrapper raises (breaking every load on a machine with a known GPU), caches it, or maps it to `None`; a TTS family without a table that got cached as "unsupported" would lose its GPU tier for three generations.
  *Fix:* State in §3.3: `SK_ERR_NOT_FOUND` → `None`, not cached (the call is cheap once the device is up); `SK_ERR_INVALID_ARGUMENT` → programming error: raise under `SOKUJI_WIRE_STRICT`/tests, `None` in production with one `logging.warning`. Add both to the §4 sidecar-accel list and a catalog test that every TTS card's `(stage, graph_family)` has a table (the only stage where a missing table changes behaviour).

- **[low] `op_coverage` names two callables of different arity, and the read-only variant returns a dict where `OpCoverage` is expected** (`§3.3 "`accel.op_coverage(machine, device_index, stage, family, compute_type) -> OpCoverage | None`" vs "`op_coverage: Callable[[int, str, str, str], OpCoverage | None]`" and "pass a read-only callable (`cache.get`; miss → `None`)"; §4 characterisation "monkeypatches `accel.op_coverage`"`) — The module function takes `machine` first and five arguments; the injected callable takes four; `cache.get` takes one key string and yields the stored `{"allSupported", "unsupported"}` dict, not an object with `.all_supported`. The round-2 fix asked for a single name for the coverage callable; the draft reuses one name for three shapes, so the characterisation fixture's `monkeypatch(accel.op_coverage)` is ambiguous about which is patched.
  *Fix:* Name them: `accel.compute_op_coverage(machine, index, stage, family, compute_type)` (native + cache write), `accel.op_coverage_for(machine) -> Callable[[int, str, str, str], OpCoverage | None]` (binds machine, computes), `accel.cached_op_coverage(machine) -> same callable` (read-only, wraps `cache.get` and builds `OpCoverage`). Define `OpCoverage` once as a frozen dataclass `(all_supported: bool, unsupported: tuple[str, ...])`.

- **[low] Op-check name spelling for single-source ops is never defined, and the 64-entry `sk_op_coverage` cap truncates silently** (`§3.2 `sk_op_check` comment "OP[src0,src1]", e.g. "MUL_MAT[q4_K,f32]"; §3.3 cached value example "NORM[f32,-]"; §3.2 dump format "one `OP[src0,src1]` per line"; `sk_op_coverage { int32_t n_ops; /* entries written */ … ops[64] }``) — The `-` placeholder for an absent `src1` appears only in §3.3's example; the CTest diff, the dumps and the wire `unsupported[]` all compare these strings, so one unstated spelling difference makes every single-source op mismatch. `n_ops` "entries written" with a fixed 64-slot array means a table longer than 64 (a TTS family with vocoder convs, im2col, FA variants and per-rung MUL_MATs is plausibly close) is cut off and `all_supported` is computed over a prefix without any error.
  *Fix:* Define the spelling once in §3.2 (`OP[src0,src1]`, `-` for an absent source, type names from `ggml_type_name`) and reference it from §3.3/§3.4. Either size the array to the largest table with a `static_assert` in `sk_ops.cpp`, or return `SK_ERR_INTERNAL` when a table exceeds the cap; add the check to the §4 CTest list.


### Lens: fresh-eyes (17)

- **[high] Paravirtual gate uses a feature bit, contradicting premise 6, the §3.1 comment and §5** (`§3.3 "Paravirtual (R36)" vs §1.1 premise 6, §3.1 enum comment on SK_FEAT_MTL_*, §5 bullet 3`) — §1.1(6) says "the raw feature bits in the profile are reported for diagnostics and never gate anything", the SK_FEAT_MTL_* comment says "Diagnostics only; §3.2 is the gate", and §5 puts "any use of the raw feature bits beyond diagnostics" out of scope. §3.3 then makes `_tier_available`'s Metal branch refuse the tier when `mtl_simdgroup_reduction` is absent from `DeviceProfile.features` — a feature-bit gate, applied to every stage (asr/translate too), on the ready path, with no `sk_device_supports_ops` call involved.
  *Fix:* Either (a) rewrite premise 6 / the §3.1 comment to carve out the two MTL bits explicitly ("derived from supports_op(NORM[f32]) / supports_op(MUL_MAT[bf16,f32]), so they ARE op coverage; the Vulkan bits are raw and gate nothing") and say in §3.3 that this is the one profile-derived gate outside `_deployment_available`, or (b) express the paravirtual refusal as a coverage entry (`ops:<idx>:tts:<any>:*` containing NORM) and accept that it only fires after the first resolve. Pick one; today the three sections cannot all be true.

- **[high] test_ops_coverage's recording backend cannot be handed to the engines through the sk_ / engine APIs** (`§3.2 first bullet ("hands that backend to each engine for one forward pass")`) — All three loaders take registry devices, not backends: sk_translate.cpp:178-184 puts a `ggml_backend_dev_t` from `sk::devices()` into `llama_model_params.devices`; sk_asr.cpp:120 does the same for transcribe.cpp; sk_tts.cpp:386-388 gives audio.cpp only a `core::BackendType` enum plus a registry-relative index (`backend_relative_index`, sk_tts.cpp:161-167, resolved inside audio.cpp's `engine/framework/core/backend.cpp: find_device_by_backend_type`). A bare `ggml_backend_i` wrapper around the CPU backend is unreachable from any of them, and audio.cpp — the one engine where the gate matters — selects by backend type, so even a registered custom device would not be chosen. Also, llama.cpp/transcribe.cpp always keep the real CPU backend in the scheduler; a recorder whose `supports_op` is not "accept everything" silently misses nodes routed to real CPU.
  *Fix:* Specify the mechanism: (1) register the recorder as a `ggml_backend_reg` + device via `ggml_backend_register()` (ggml-backend-impl.h) whose `supports_op` returns true for every node and whose `graph_compute` records then forwards to the real CPU backend; (2) for audio.cpp, a test-build-only patch spec (same `cmake/patch_upstream.py` mechanism, e.g. `native/patches/audio.cpp-test-recorder.json`) that makes `init_backend` accept a recorder device, or record at the `ggml_backend_graph_compute` boundary with a test-only shim; (3) say the test drives llama.h / transcribe / audio.cpp directly, not the `sk_*` loaders. Note too that native/src/audiocpp_compat.h:42-43 includes `ggml.h`/`ggml-impl.h`, not `ggml-backend-impl.h` — the include-path claim holds only because both live in ggml/src.

- **[high] WEIGHT single-dtype substitution mis-describes mixed-quant rungs** (`§3.2 paragraphs 2-3 (WEIGHT placeholder, "a device that refuses one dtype loses only that rung")`) — `q4_k_m → Q4_K` etc. assumes one weight dtype per rung. llama.cpp/transcribe.cpp K-quant *_M mixes are not: Q4_K_M files carry Q6_K (attn_v/ffn_down subsets, output.weight) and Q5_K tensors, Q5_K_M carries Q6_K, and `token_embd`/`output` are often Q8_0/F16. So the substituted table for `q4_k_m` omits `MUL_MAT[q6_K,f32]`/`GET_ROWS[q8_0,...]` that the real graph executes; a device refusing Q6_K would pass the q4_k_m check and abort, and one refusing Q4_K would lose q5_k_m needlessly. The recording test can only catch this for the rung of the cached model, and every cached ASR/translate model on disk is Q8_0 (`~/.cache/sokuji-native-tests/whisper-tiny-Q8_0.gguf`, `moonshine-streaming-tiny-Q8_0.gguf`, `Qwen3-0.6B-Q8_0.gguf`).
  *Fix:* Replace the 1:1 map with a rung → weight-type *set* (e.g. q4_k_m→{Q4_K,Q5_K,Q6_K,Q8_0,F16}, q5_k_m→{Q5_K,Q6_K,Q8_0,F16}, q6_k→{Q6_K,Q8_0,F16}, q8_0→{Q8_0,F16}, f16→{F16}, bf16→{BF16,F16}), expanding each WEIGHT entry into one check per type — or, better, have the dump/recording step read the actual tensor-type set from the GGUF per (family, rung) and store it in the `.ops` file. State which, and add a per-rung test model (or dump) requirement to the pin-bump checklist.

- **[high] `variants[].supported: False` for a refused TTS rung contradicts "resolves to its cpu row"; `tiers[].available` cannot carry a per-rung verdict** (`§3.3 "It replaces the direct _tier_available check at … the three wire producers (`_h_models_catalog`'s `tiers[].available` and `variants[].supported`)"; §4 sidecar accel last bullet`) — In accel.py `_h_models_catalog`, `variants[].supported` means "runs on this machine at all" (comment at ~818-835: GGUF cards are unconditionally True because the cpu tier is the safety net) and the renderer uses it to gate downloads. A TTS rung whose GPU tier is refused still runs on cpu per §3.3, so `supported: False` would hide a rung the sidecar will happily load. Separately, `tiers[]` is deduplicated per tier across rungs (accel.py:768-776, `seen_tiers`), so a per-(rung, tier) gate has no place to land: with q8_0 supported and bf16 refused on gpu-vulkan, is `gpu-vulkan.available` true or false? The spec never says.
  *Fix:* Keep `variants[].supported` untouched. Add an explicit per-variant field (e.g. `unsupportedTiers: string[]` or `gpuOk: boolean`) to `models_catalog_result` + `wire_schema.json` + `NativeModelsCatalogResultMsg`, and define `tiers[].available` as "true if any listed rung can execute on that tier" (or "the default rung"). Update the §4 test to assert the new field, not `supported: False`.

- **[medium] Op coverage: lazy callable inside the planner vs "never inside the pure planner"** (`§3.2 last paragraph vs §3.3 "Op coverage cache … They pass the results to the planner as an injected callable" and `_deployment_available` calling `op_coverage(...)``) — §3.2 places the (seconds-long, exception-throwing) Vulkan first-init "in the accel.resolve_* wrappers … never inside the pure planner". §3.3 says the wrappers compute coverage "for the resolved card's graph_family and each compute_type … and pass the results … as an injected callable", but `_deployment_available` then *calls* `op_coverage(dev.index, stage, family, ct)` from inside the planner. If the injected callable is `functools.partial(accel.op_coverage, machine)`, the planner performs native calls and `bench_save` file writes; if it is a dict lookup over precomputed results, the wrapper must enumerate every (device, compute_type) up front — the spec's wording supports both readings.
  *Fix:* State it: the wrapper eagerly computes `{(idx, stage, family, ct): OpCoverage|None}` for each known non-cpu device and each compute_type on the card *before* calling the planner, and passes `results.get`-shaped callable; the planner never triggers native. Add a planner test that the callable is only ever invoked with keys the wrapper precomputed (or make the planner take the dict directly and drop the callable indirection).

- **[medium] `_engine_identity`'s `preferredDevice` cannot use `_deployment_available` — it has no model or deployment** (`§3.3 "It replaces the direct `_tier_available` check at … `_engine_identity`'s `preferredDevice`"`) — accel.py:717-721 ranks native devices with `_tier_available(tier, m)` — there is no card and no `Deployment` in scope, while the spec's `_deployment_available(model, d, machine, *, op_coverage)` needs both (`model.graph_family`, `d.backend`, `d.compute_type`). Likewise `_h_list_variants`'s only `_tier_available` call (accel.py:660) is in the non-GGUF branch, dead for every current translate card.
  *Fix:* Remove `_engine_identity` from the replacement list (it is touched only by the `_tier_available` paravirtual change) and say `_h_list_variants` is affected only through `select_variant`.

- **[medium] Callable threading misses the two planner functions `_h_models_catalog` calls directly** (`§3.3 "keyword-only, threaded through the four public entry points (`resolve`, `resolve_translate`, `resolve_tts`, `select_variant`)"`) — `_h_models_catalog` calls `_tc_pick_quant(mdl, m, None, budget)` (imported from planner at accel.py:182-187, used at ~803) and `accel._llamacpp_variant_row` (accel.py:274 → `planner._llamacpp_variant_row`) directly, never via the four entry points. Both contain gates the spec says `_deployment_available` replaces (`gpu_possible` at planner.py:273 and 512, `_row()` at 500-508). Without an `op_coverage` keyword on those two, the catalog cannot pass its read-only callable and `models_catalog_result`'s recommended rung diverges from what `resolve_tts` loads.
  *Fix:* List six sites: the four entry points plus `_llamacpp_variant_row` and `_tc_pick_quant` (and the `accel._llamacpp_variant_row` wrapper), each gaining `*, op_coverage` with a `lambda *a: None` default so existing tests stay valid.

- **[medium] `_native_generation() -> str` takes no arguments but the generation formula hashes the profiles** (`§3.3 "Two new detectors … `_native_profiles()` … and `_native_generation() -> str`" vs the `generation = blake2s(...)` block`) — The formula includes `[(d.kind, d.device_uuid, d.driver_name, d.driver_version) for d in devices]`, but `_native_generation` is a separate zero-arg detector each wrapped in `_safe`. Either it re-calls `sk_device_profile_get` itself (double native calls, and the two detectors can disagree when one raises) or `probe()` composes it — the spec says "probe() wraps both in `_safe`", which only fits the first. The 1.0.x degrade sentence ("generation is still computed from sk_version and engine_versions") also leaves it open whether `devices=()` contributes an empty list or is omitted, which changes the hash.
  *Fix:* Define `_native_identity() -> tuple[str, dict]` (sk_version, engine_versions) as the detector, and compute `generation` in `probe()` from `(identity, devices, GGML_* env)` with `devices=()` hashing as the empty list. Keep `generation=""` only when the identity detector raises.

- **[medium] `bench_save` has no way to know the current generation, and the prune rule deletes non-generation keys that existing tests write** (`§3.3 "The cache file gains a `_generations` list … `bench_save` appends the current generation on first save under it, prunes every entry whose prefix is not among the last three"; §4 "test_accel.py:413, 430 move to `_cache_key`"`) — `bench_save(cache)` (accel.py:500) receives only the dict, and `bench_load` is specified to strip `_generations` before returning, so at save time neither the current generation nor the previous list is available. The prune rule as written removes every key whose prefix is not a recent generation — including plain `_bench_key` keys — which breaks `test_bench_cache_roundtrip` (test_accel.py:393-398) and `test_bench_key_is_stable_and_distinct` (407-410), neither listed in §4. §4's line numbers are also off: 413 is `test_measure_rtf_runs_and_caches` (its `_bench_key` use is at 427) and the test at 430 contains no `_bench_key` call.
  *Fix:* Specify `bench_save(cache, *, generation)` that re-reads `_generations` from disk (or make `bench_load` return `(entries, generations)`), and define pruning as "drop keys whose `|`-prefix is a *known generation* not among the last three; keys with no generation prefix are dropped only when a `_generations` list already exists". List 393-398, 407-410 and 427 as the test_accel sites to update.

- **[medium] "override='gpu' resolves the refused TTS rung to its cpu row" holds only with a pin or a single-rung card** (`§3.3 "A pin, a downloaded rung and an explicit `override=\"gpu\"` therefore all resolve a TTS rung the device cannot execute to its cpu row"; §4 "three tests, one per bypass path"`) — `planner.resolve_tts`'s override branch (planner.py:420-428) narrows the model to one rung only when `pin` is given; otherwise `_resolve_model → resolve_deployments` keeps the *other* rung's gpu row in `usable`, `_pinned` puts it first, and the plan leads with e.g. q8_0-on-vulkan — the refused bf16 rung never appears at all, on any tier. The sentence and the third §4 test are therefore true only for `override="gpu", pin=<rung>` or a card with one rung (supertonic-3).
  *Fix:* Rewrite as "under `override='gpu'` with that rung pinned (or on a single-rung card)" and make the §4 test pass `pin=`. If the intent is that an unpinned gpu override should still surface the refused rung on cpu, that is a new ranking rule and belongs in spec B.

- **[medium] §4 CTest asserts the Metal bits on "Apple Silicon", which CI's only macOS runner (paravirtual) fails by design** (`§4 native (CTest): "on Apple Silicon `SK_FEAT_MTL_SIMDGROUP_REDUCTION | SK_FEAT_MTL_BFLOAT | SK_FEAT_UMA` are set"`) — §1 and §2 establish that the paravirtual Metal device lacks simdgroup reduction (it aborted on `GGML_OP_NORM`; `has_simdgroup_reduction` is Apple7|Metal3, which the VM shim does not report). CLAUDE.md states every CI macOS runner is that VM. An unconditional assertion turns the mac-arm64 CTest lane red permanently.
  *Fix:* Gate the assertion on the device description not matching `paravirtual`, and add the inverse assertion for the paravirtual case (`known == 1`, `SK_FEAT_MTL_SIMDGROUP_REDUCTION` clear) — that is the structured R36 signal the spec wants, and CI is the one place it can be exercised.

- **[low] `OpCoverage` and the absent-src1 name format are used but never defined** (`§3.2 `sk_op_check` comment ("OP[src0,src1]", e.g. "MUL_MAT[q4_K,f32]") vs §3.3 "NORM[f32,-]"; §3.3 `accel.op_coverage(...) -> OpCoverage | None`, `cov.all_supported``) — The C struct's name format shows only two-source ops; the `-` placeholder for a unary op appears first in §3.3 with no rule (and what about ops with src2, e.g. FLASH_ATTN_EXT with mask, ROPE with positions?). `OpCoverage` (Python) has no definition; its fields are inferred from `cov.all_supported` and the cached `{allSupported, unsupported}` dict.
  *Fix:* In §3.2 define: `OP[src0,src1]`, `-` for an absent source, only src0/src1 recorded (say why src2+ are ignored). In §3.3 add `@dataclass(frozen=True) class OpCoverage: all_supported: bool; unsupported: tuple[str, ...]` and the cache-dict ↔ dataclass mapping.

- **[low] §2 overstates what the CPU registration exposes through get_proc_address** (`§2 Native bullet: "for the CPU and Metal registrations that is `ggml_backend_get_features` alone"`) — ggml's CPU reg `get_proc_address` answers ~10 names (`ggml_backend_set_n_threads`, `ggml_backend_dev_get_extra_bufts`, `ggml_backend_get_features`, `ggml_backend_set_abort_callback`, `ggml_backend_cpu_numa_init`, `ggml_backend_cpu_is_numa`, `ggml_backend_cpu_set_use_ref`, `ggml_threadpool_new/free`, `ggml_backend_cpu_set_threadpool`); only Metal is get_features-alone (ggml-metal.cpp `get_proc_address`). Checked on the ggml tree at ~/Desktop/kizunaai/synthesize.cpp/ggml (v0.15.2 — the pinned 0.22.0 is not on disk), so re-verify against the pin; the design conclusion (no `ggml_get_type_traits_cpu` / `supports_family` reachable) still stands.
  *Fix:* Say "neither registration exposes `ggml_get_type_traits_cpu` or `ggml_backend_metal_supports_family`; the CPU one exposes threads/threadpool/NUMA/features hooks, Metal only `ggml_backend_get_features`".

- **[low] §2 misdescribes today's check_linux_deps.py gate as per-module** (`§2 Native bullet: "The Linux wheels allow a `libvulkan.so.1` DT_NEEDED on the dlopen'd ggml-vulkan module only"`) — native/ci/check_linux_deps.py:21-23 and :60 apply one global `ALLOWED` set (which contains `libvulkan.so.1`) to every staged shared object; nothing is per-file. A `libvulkan` DT_NEEDED on `libsokuji_native.so` would pass today. §3.1's "asserts that for libsokuji_native.so while keeping its allowance for the ggml-vulkan module" is therefore a new per-file rule, not a tightening of an existing one.
  *Fix:* In §2 write "allows `libvulkan.so.1` on any staged object"; in §3.1/§4 describe the per-file rule being added (a `DENY_BY_FILE = {"libsokuji_native.so": {"libvulkan.so.1"}}` or similar).

- **[low] `_STAGE_OF_BACKEND[d.backend]` KeyErrors on fixture backends once a test supplies a known device** (`§3.3 gate code, `stage = _STAGE_OF_BACKEND[d.backend]``) — Tests build deployments with backends like "be" (test_platform_filter.py:11) and "ctranslate2" (test_accel.py:408, 421). Today they never reach the lookup because fixture Machines have `devices=()`; the new §4 tests that add known profiles to such fixtures will hit the KeyError instead of a gate result.
  *Fix:* `stage = _STAGE_OF_BACKEND.get(d.backend); if stage is None: return True` (unknown backend → pass-through, premise 5), and say so.

- **[low] Fixed buffers in `sk_device_profile` / `sk_op_coverage` can truncate silently** (`§3.1 `char cpu_features[256]`, `char driver_version[64]`; §3.2 `sk_op_check ops[64]``) — x86 CPU `ggml_backend_get_features` lists ~20 entries (SSE3, SSSE3, AVX, AVX_VNNI, AVX2, BMI2, F16C, FMA, AVX512, AVX512_VBMI, AVX512_VNNI, AVX512_BF16, AMX_INT8, AMX_BF16, OPENMP, REPACK, LLAMAFILE, …) — as `NAME=value,` pairs that approaches or exceeds 256 bytes. Vulkan's `driverInfo` is `VK_MAX_DRIVER_INFO_SIZE` = 256. A family table with more than 64 (op, src0, src1) entries after WEIGHT expansion (see the mixed-quant finding) overflows `ops[64]`; `n_ops` "entries written" does not say whether the rest are dropped or the call fails.
  *Fix:* Size `cpu_features[512]` and `driver_version[256]` (or document truncation as a hard rule), and make `sk_device_supports_ops` return `SK_ERR_INTERNAL` when a table exceeds the array — with a CTest that every table fits.

- **[low] §3.2's test-model inventory understates what is cached, weakening the case for the dump fallback** (`§3.2 first bullet: "(~/.cache/sokuji-native-tests/: whisper-tiny, moonshine-streaming-tiny, Qwen3-0.6B, moss-tts-nano, supertonic-3 today)"`) — `~/.cache/sokuji-native-tests/tts/` holds all nine TTS families (index-tts2.5, irodori-tts-v4-small, moss-tts-nano, omnivoice-0.6b, pocket-tts-en, qwen3-tts-0.6b, supertonic-3, voxcpm1-0.5b, voxcpm2) plus a `tts-bf16/` set, so every audio.cpp family — the only stage where the gate fires — can be recorded live; the dump mechanism is needed only for the ASR/translate architectures without a cached model.
  *Fix:* Correct the inventory, and scope the `.ops` dump requirement to asr/translate families; require live recording for all nine TTS families on every pin bump.


## Round 4 — third draft, fresh-eyes final pass


### Lens: engineer (8)

- **[high] Op tables keyed on (op, src0, src1) cannot reproduce the predicates they claim to answer** (`§3.2 (spelling comment, "one representative tensor per entry", "src2+ ... never decide support"), §3.3 gate `cov.all_supported`, §4 `test_ops_coverage``) — Both backends' `supports_op` read more than the two source dtypes. In the ggml tree on disk (synthesize.cpp/ggml, same predicate structure as 0.22) `ggml_backend_vk_device_supports_op` reads `op->type` (dst), `->ne[]` or `op_params` at 41 sites — e.g. `IM2COL` is `src1 == F32 && (op->type == F32 || F16)`, `CPY/DUP` branch on dst type, `FLASH_ATTN_EXT` on head size, `ROPE` on mode, and every op is first rejected when `ggml_nbytes(src)` exceeds `max_buffer_size`/`maxStorageBufferRange`; Metal's `ggml_metal_device_supports_op` does the same at 26 sites (NORM needs `ggml_is_contiguous_rows(src0)`, CPY switches on dst type). A hand-written `OP[src0,src1]` entry probed with an unspecified scratch tensor can therefore answer differently from the real node, in either direction. Because the TTS gate fails closed (`_ABORTS_ON_UNSUPPORTED`), a false "unsupported" silently moves a family to the cpu row on real hardware, and the recorder only checks that the recorded `(op, src0, src1)` set is ⊆ the table — it cannot detect a wrong answer caused by dst type, shape or params.
  *Fix:* Make the recording the manifest: have the recording device / `sk_recording_graph_compute` capture the full node descriptor (op, dst type, src0..src2 types, ne[0..3] for src0/src1/dst, op_params, contiguity flags), de-duplicate, and write that to `native/src/ops/<stage>-<family>.ops`; `sk_device_supports_ops` rebuilds each recorded node exactly (with `WEIGHT` substituted into src0 type only, and ne[0] chosen as the recorded value so K-quant block sizes stay valid) and calls `supports_op` on it. Change the spelling to include dst (`OP[src0,src1]->dst`) so `unsupported[]` on the wire names the real node.

- **[medium] `bench_load()` shape change breaks the frozen characterisation fixture the spec says must not move** (`§3.3 "Cache file" (`bench_load()` returns `(entries, generations)`), premise 5 / §2 "pins only `_downloaded_quants` and `bench_load`", §4 characterisation`) — `sidecar/tests/test_characterization.py:96` monkeypatches `accel.bench_load` to `lambda: {}` and the accel wrappers pass `cache=bench_load()` straight into the planner (accel.py:237, 247, 264). If `bench_load()` starts returning a tuple, the wrappers must unpack it, and the fixture's `{}` then raises (`ValueError: not enough values to unpack`) in every matrix row — the file the spec says must not change has to change.
  *Fix:* Keep `bench_load() -> dict` (entries only, as today) and add a second function (`bench_read() -> tuple[dict, list[str]]`) used only by `bench_save`/`compute_op_coverage`; or state explicitly that `test_characterization.py`'s autouse fixture becomes `lambda: ({}, [])` and drop the "pins only … `bench_load`" invariant.

- **[medium] `variants[].unsupportedTiers` cannot be "added to wire_schema.json" or pinned by the consistency test** (`§3.4 first paragraph ("all added to `wire_schema.json` … both fail otherwise"), §4 renderer ("pins the three optional fields against `wire_schema.json`")`) — `wire_schema.json` and `wire.validate_outbound` are top-level-only by design (wire.py docstring: "Values and nested payload shapes … are deliberately out of scope"); `models_catalog_result` is `{required: [id, models], optional: []}` and `nativeProtocol.consistency.test.ts` compares only top-level TS members (`extractServerMsgFields`). A nested `models[].variants[].unsupportedTiers` has no place in either, so the plan step as written cannot be executed and the promised test cannot exist.
  *Fix:* Rewrite §3.4/§4: only `generation` and `devices` are added to `wire_schema.json` `hardware_info_result.optional` and pinned by the consistency test; `unsupportedTiers` is an optional member of the TS `NativeModelInfo['variants'][number]` type, covered by a `nativeModelStore` test that feeds a catalog fixture carrying it.

- **[medium] The op-coverage callable is not threaded through the function that owns the first-listed gate** (`§3.3 "The callable is threaded … through the **six** planner functions" vs. "It replaces the direct `_tier_available` check at … the `usable` comprehension in `resolve_deployments`"`) — The `usable` comprehension lives in `planner.resolve_deployments` (planner.py:160), reached via `_resolve_model` (187) from `resolve`, `resolve_translate`'s override branch and `resolve_tts`'s override branch, and also exposed as `accel.resolve_deployments` (accel.py:225). Neither `resolve_deployments`, `_resolve_model` nor that wrapper is in the list of six, so the `override="gpu"` + pin → cpu-row test in §4 has no path by which the callable reaches the gate.
  *Fix:* Add `resolve_deployments`, `_resolve_model` and `accel.resolve_deployments` to the threading list (keyword-only, same `lambda *a: None` default) and restate the count.

- **[medium] Vulkan device-matching helper omits two of ggml's selection rules** (`§3.1 Vulkan bullet, "Matching ggml's device `i` to a physical device replicates ggml's own selection"; §6 "Dual-ICD and identical GPUs"`) — `ggml_vk_instance_init` only admits physical devices whose `deviceType` is `eDiscreteGpu`/`eIntegratedGpu` **and** pass `ggml_vk_device_is_supported`, then de-duplicates by UUID/driverID, and only if that leaves nothing falls back to the first non-CPU device. The spec's rule (UUID group → driverID priority → `GGML_VK_VISIBLE_DEVICES` → positional) skips the type filter and the support check, so on any Linux box with Mesa's llvmpipe (a `eCpu` Vulkan device, present wherever mesa-vulkan-drivers is installed) or a device ggml rejects, the helper's list is longer than ggml's and positional matching is wrong; the "counts differ → description match" fallback hides the bug only while descriptions are unique.
  *Fix:* Replicate the full filter: drop `eCpu`/`eOther`/`eVirtualGpu` devices, apply the same extension/feature test ggml's `ggml_vk_device_is_supported` makes (pin its rule the same way as the driver table), then UUID/driverID dedup, then `GGML_VK_VISIBLE_DEVICES`, then the no-GPU fallback; add a helper test with a fake llvmpipe entry in front of a real GPU.

- **[medium] Auto-mode fit walk still selects a refused rung, landing a GPU-capable card on CPU** (`§3.3 "Consequences" paragraph and premise 2 ("this spec only guarantees the candidates B ranks are ones the device can run") vs. `_llamacpp_variant_row``) — `_llamacpp_variant_row` builds `quants` from every deployment and runs `_fit_walk` before calling `_row(picked)`; only `_row` consults the gate. With nothing downloaded, a budget that fits bf16, and `MUL_MAT[bf16,f32]` refused, the walk picks bf16 and `_row("bf16")` returns bf16's cpu row — although q8_0's GPU row is available. The §4 test ("bf16 refused while its q8_0 sibling is kept") holds at `_deployment_available` level but the resolve outcome contradicts the premise; the spec only addresses the override="gpu" case ("stays spec B's business").
  *Fix:* State the rule: when `gpu_possible`, restrict `quants` (and `sizes` in `_tc_pick_quant`) to rungs that have at least one `_deployment_available` GPU row before the fit walk, or explicitly accept the CPU landing in §6 and add the matrix case to §4 so the behaviour is pinned either way.

- **[low] Legacy-key pruning rule in `bench_save` is stated two contradictory ways and keys are shape-indistinguishable** (`§3.3 "Cache file"`) — "keys with no generation prefix are dropped only when a `_generations` list already exists" and, in the same sentence, a legacy flat file (no list) "is dropped at the first save" — at the first save no list exists yet, so the first clause forbids what the second requires. Also a legacy key `fp|model|backend|device|ct` and a new key `gen|fp|model|…` both start with a 12-hex segment, so "no generation prefix" cannot be decided by shape.
  *Fix:* Define: on save, a key is kept iff its first `|` segment is in the post-rotation `_generations` list; every other key (legacy or rotated-out) is dropped. This covers the legacy file (empty list before append) and needs no shape test.

- **[low] ASR arch-equality test is defined on cards that do not exist for two of the cached models** (`§3.3 "a test asserts equality for every cached test model", §4 sidecar catalog ("for each cached test model, `sk_asr_caps.arch` equals the card's `graph_family`")`) — The cache holds whisper-tiny, moonshine-streaming-tiny and Qwen3-0.6B; only `moonshine-streaming-tiny` is a catalog card (catalog.py:377). There is no `whisper-tiny` ASR card (whisper-base/medium/large-v2/v3/turbo exist) and no Qwen3-0.6B translate card, so "the card" is undefined for two of three.
  *Fix:* Define the test by architecture, not card id: for each cached ASR model, `sk_asr_caps.arch` must equal the `graph_family` of every `_tc_row` sharing that architecture (whisper-tiny → the whisper cards); for the translate model, read `general.architecture` from the GGUF and assert it matches the `arch=` of the same-architecture `_llm_translate_row`s.


### Lens: skeptic (8)

- **[high] Rung→dtype table does not match the real TTS GGUFs: q8_0 files carry BF16 tensors, every file carries F32/I64/I32** (`§3.2 'Rungs are dtype sets, not dtypes' table and `test_ops_coverage`'s ⊆ assertion; §4 planner test 'a bf16 rung whose MUL_MAT[bf16,f32] is unsupported is refused while its q8_0 sibling is kept'`) — Read the tensor-type tables of all 16 cached GGUFs under ~/.cache/sokuji-native-tests. The q8_0 files of five of the nine audio.cpp families contain BF16 weight tensors: moss-tts-nano-100m-q8_0 (Q8_0 181, BF16 108, F16 32, F32 247), qwen3-tts-12hz-0.6b-base-q8_0 (Q8_0 249, BF16 212), pocket-tts-english-q8_0 (BF16 136 > Q8_0 67), voxcpm2-q8_0 (BF16 140), index-tts2_5-q8_0 (BF16 113). Every file also carries F32 (norms/biases), and omnivoice/index-tts2.5/supertonic-3 carry I64, voxcpm2 I32. Against the spec's `q8_0 → {Q8_0, F16}`: (a) the CTest 'actual tensor-dtype set ⊆ rung set' fails for all 16 files as written; (b) the gate is wrong in the direction it exists for — a q8_0 TTS rung on a Metal device where `has_bfloat` is false (GGML_METAL_BF16_DISABLE, which §3.3 itself hashes into the generation; Metal refuses any node with a BF16 src, ggml-metal-device.m:1411-1421) passes `all_supported` because no BF16 entry is expanded, then audio.cpp aborts; the §4 test that keeps the q8_0 sibling enshrines that for five families.
  *Fix:* Stop deriving the dtype set from the rung name. Read the tensor-type set from the GGUF header (header-only read, cheap; the sidecar already knows the file path at resolve time) and pass it to native: `sk_device_supports_ops(index, stage, family, const char *const *dtypes, int n_dtypes, out)`, expanding `WEIGHT` over that set; keep the rung table only as the fallback when the file is not on disk yet, and make the fallback sets include BF16 for q8_0. Put F32 (and I32/I64 for GET_ROWS/index tensors) in the fixed entries and exclude non-weight types explicitly from the ⊆ assertion instead of pretending they are absent.

- **[high] Vulkan physical-device→ggml-index matching replicates the wrong selection rule** (`§3.1 Vulkan bullet, 'Matching ggml's device i to a physical device replicates ggml's own selection'; §6 'Dual-ICD and identical GPUs'`) — ggml v0.22.0 `ggml_vk_instance_init` (ggml-vulkan.cpp:7441-7581) does not do what the spec describes. (1) When `GGML_VK_VISIBLE_DEVICES` is set the listed numbers are raw `enumeratePhysicalDevices` indices with NO de-duplication and NO type filter (7444-7459) — the spec applies the env after dedup. (2) Otherwise only devices whose `deviceType` is eDiscreteGpu or eIntegratedGpu AND that pass `ggml_vk_device_is_supported` (storageBuffer16BitAccess, 18925-18937) are considered (7476); everything else — lavapipe/llvmpipe (deviceType CPU, present on every Mesa install), virtual GPUs, 16-bit-storage-less devices — is dropped, so a purely positional match on the loader's full list is off by one on any Linux box with lavapipe enumerated before or after the real GPU. (3) Grouping is deviceUUID OR (deviceLUIDValid && equal LUID) and is skipped when both drivers are MoltenVK (7494-7501). (4) The priority table also has Qualcomm (proprietary 1 > Turnip 2) and Dozen 100 is global. (5) If the filtered list is empty ggml falls back to the first non-CPU device (7566-7575). The spec's 'fall back to description matching only when the counts differ' hides the mismatch rather than fixing it; `driver_name`/`device_uuid` are hashed into the generation, so a wrong match silently mis-keys the bench cache.
  *Fix:* Specify the helper as the exact sequence: if `GGML_VK_VISIBLE_DEVICES` is set → those raw indices, no filtering; else filter by (deviceType ∈ {discrete, integrated}) ∧ storageBuffer16BitAccess (query VkPhysicalDeviceVulkan11Features) → dedup by UUID ‖ LUID unless both driverIDs are MoltenVK, with the per-vendor tables including Qualcomm → if empty, first device with deviceType ≠ CPU. Then match positionally, no description fallback (log a hard mismatch and set `known = 0` instead). Pin the whole sequence to ggml-vulkan.cpp:7441-7581, not just the priority table.

- **[medium] `OP[src0,src1]` spelling cannot construct the scratch node for ops whose support depends on the op kind or on src[2..4]** (`§3.2 'Spelling … Only src0 and src1 are recorded: src2+ … never decide support'`) — Both backends decide support on data the spelling omits. UNARY and GLU: Vulkan (18082-18126) and Metal (1433-1480) switch on `ggml_get_unary_op`/`ggml_get_glu_op` and return false for kinds not listed — `UNARY[f32,-]` says nothing about GELU vs SOFTPLUS vs XIELU, and audio.cpp vocoders use several. FLASH_ATTN_EXT: Vulkan requires `fa_kv_ok(src[2])` (V type), src[3] mask F16, src[4] sinks F32, and `(K is BF16) == (V is BF16)` (18196-18234); Metal's global BF16 refusal scans src[0..2] (1416-1420). MUL_MAT_ID reads src[2] as `a` in the Vulkan check (18175). So the recorded key, the table key and the scratch constructor cannot be the same string, and two families with different unary kinds collapse to one entry.
  *Fix:* Make the table entry a constructor spec, not a spelling: `{op, op_param (unary/glu kind, rope mode), src types[0..4] with WEIGHT placeholders, minimal ne}`; derive the wire/cache spelling from it (e.g. `UNARY.GELU[f32,-]`, `FLASH_ATTN_EXT[f32,f16,f16,f16]`). Have the recorder capture `op_params` and all five src types.

- **[medium] Vulkan supports_op is shape/size-dependent; a placeholder-shaped scratch tensor can answer 'supported' for a node the real graph refuses, and audio.cpp still aborts** (`§3.2 'Each entry is answered by ggml_backend_dev_supports_op on a scratch ggml_context … so the answer is … the check ggml's scheduler makes per node'; §3.3 `_ABORTS_ON_UNSUPPORTED``) — `ggml_backend_vk_device_supports_op` (18051-18079) first rejects any src or dst whose `ggml_nbytes` exceeds `device->max_buffer_size`, and — unless the op uses BDA or `shader_64b_indexing` — `maxStorageBufferRange`; MUL_MAT_ID additionally depends on shared-memory-sized pipeline availability (18132), ROPE/CPY on contiguity, FLASH_ATTN_EXT on HSK/HSV % 8. None of that is a function of (op, src0 type, src1 type). A representative tensor of arbitrary small shape passes, the real embedding or vocoder weight does not, and on the TTS path the outcome is the GGML_ABORT the gate exists to prevent (older Intel gen9 ANV reports maxStorageBufferRange = 128 MiB; Mali/Adreno similar). The spec equates the scratch answer with the scheduler's per-node answer; that equality only holds with the real node's shapes.
  *Fix:* Record per entry the largest src/dst `nbytes` (and the FA head sizes) seen in the recorded graph and size the scratch tensors to those maxima per (family, rung); or reduce the claim: state in §3.2/§6 that the check is dtype-level and cannot catch buffer-range limits, and keep the R36-style description rule as the backstop for those.

- **[medium] Feature structs are chained unconditionally; ggml chains each only when the device advertises the extension, and two of the structs need Vulkan-Headers ≥ 1.4.311** (`§3.1 Vulkan bullet: 'reads vkGetPhysicalDeviceFeatures2 (float16-int8, bfloat16, integer-dot-product, cooperative-matrix, cooperative-matrix-2) with those structures chained'`) — ggml (7208-7243) enumerates device extensions first and links `VkPhysicalDeviceCooperativeMatrixFeaturesKHR`, `…ShaderIntegerDotProductFeatures`, `…ShaderBfloat16FeaturesKHR`, `…CooperativeMatrix2FeaturesNV` into the pNext chain only if the matching extension is present; passing a feature struct for an extension the ICD does not implement is invalid usage (the ICD may leave the fields uninitialised — the spec reads them into `features`). `VkPhysicalDeviceShaderBfloat16FeaturesKHR` and the NV coopmat2 struct are header-guarded in ggml (`#if defined(VK_KHR_shader_bfloat16)`, and ggml even defines its own copy at lines 99-110 for older headers); the spec compiles against whatever `find_package(Vulkan)` finds in native's scope, which on a developer box with distro headers (22.04's 1.3.204) does not have them.
  *Fix:* Enumerate `vkEnumerateDeviceExtensionProperties`, chain each feature struct only when its extension is listed, zero-initialise all structs, and guard the two newer structs with the same header macros ggml uses (bit stays clear when absent). State the minimum Vulkan-Headers version native needs, or vendor the headers via FetchContent.

- **[low] `SK_FEAT_MTL_BFLOAT` via `supports_op(MUL_MAT[bf16,f32])` measures `has_bfloat && has_simdgroup_reduction`, not `has_bfloat`** (`§3.1 enum comment on SK_FEAT_MTL_BFLOAT; §4 Metal CTest`) — Metal's MUL_MAT case is `return has_simdgroup_reduction && src0 != NVFP4` (ggml-metal-device.m:1709-1711) after the global BF16 refusal (1411-1421). On the paravirtual runner the bit therefore reads clear whether or not bfloat is supported, so the profile cannot distinguish 'no bfloat' from 'no simdgroup reduction'.
  *Fix:* Derive the bit from an op whose only gate is bfloat — `CONCAT[bf16,bf16]` returns `has_bfloat` alone (1502-1503) — or document that the bit is the conjunction.

- **[low] `driver_name[64]` is smaller than Vulkan's `VK_MAX_DRIVER_NAME_SIZE` (256), contradicting the 'sizes cover every value Vulkan defines' guarantee** (`§3.1 `sk_device_profile.driver_name[64]` and the comment above `sk_device_profile_get``) — `VkPhysicalDeviceDriverProperties.driverName` is `char[VK_MAX_DRIVER_NAME_SIZE]` = 256; `driverInfo` is 256 (which the spec sizes correctly). Truncation is harmless per se, but `driver_name` feeds the generation hash and the CTest asserts the buffers fit.
  *Fix:* Size `driver_name[256]` (or keep 64 and drop the 'covers every value' claim, noting truncation explicitly).

- **[low] The `audiocpp_compat.h` macro redirect renames ggml-backend.h's own prototype because the header is force-included before every other include** (`§3.2 audio.cpp recording mechanism: 'audiocpp_compat.h … redirects ggml_backend_graph_compute to sk_recording_graph_compute'`) — `audiocpp_compat.h` is injected with `-include` / `/FI` into every audio.cpp TU (native/CMakeLists.txt:75-77) and includes only `ggml.h` and `ggml-impl.h`; `ggml-impl.h` does not include `ggml-backend.h`. A `#define ggml_backend_graph_compute sk_recording_graph_compute` placed there is active when the TU later includes `ggml-backend.h`, so the `GGML_BACKEND_API enum ggml_status ggml_backend_graph_compute(...)` prototype itself is renamed: the test then defines a symbol already declared with `__declspec(dllimport)` on win-x64, and the shim TU (if it also gets the force-include) has no name for the real function to forward to.
  *Fix:* Under `SK_RECORD_OPS`, `#include "ggml-backend.h"` in the compat header before the `#define`, and either compile the shim TU without the force-include or `#undef ggml_backend_graph_compute` at its top. Also state that the registered recording device's `supports_buft` must return true for the CPU buffer type — `ggml_backend_sched_backend_from_buffer` (ggml-backend.cpp:884-887) requires both `supports_buft` and `supports_op` to route a weight-bearing node to it.
